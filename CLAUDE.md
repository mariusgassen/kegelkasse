# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Workflow Orchestration

### 1. Plan Mode Default
- Enter plan mode for ANY non-trivial task (3+ steps or architectural decisions)
- If something goes sideways, STOP and re-plan immediately
- Use plan mode for verification steps, not just building
- Write detailed specs upfront to reduce ambiguity

### 2. Subagent Strategy
- Use subagents liberally to keep main context window clean
- Offload research, exploration, and parallel analysis to subagents
- For complex problems, throw more compute at it via subagents
- One task per subagent for focused execution

### 3. Self-Improvement Loop
- After ANY correction from the user: update tasks/lessons.md with the pattern
- Write rules for yourself that prevent the same mistake
- Ruthlessly iterate on these lessons until mistake rate drops
- Review lessons at session start for relevant project

### 4. Verification Before Done
- Never mark a task complete without proving it works
- Diff behavior between main and your changes when relevant
- Ask yourself: "Would a staff engineer approve this?"
- Run tests, check logs, demonstrate correctness

### 5. Demand Elegance (Balanced)
- For non-trivial changes: pause and ask "is there a more elegant way?"
- If a fix feels hacky: "Knowing everything I know now, implement the elegant solution"
- Skip this for simple, obvious fixes — don't over-engineer
- Challenge your own work before presenting it

### 6. Autonomous Bug Fixing
- When given a bug report: just fix it. Don't ask for hand-holding
- Point at logs, errors, failing tests — then resolve them
- Zero context switching required from the user
- Go fix failing CI tests without being told how


## Task Management

1. Plan First: Write plan to `tasks/todo.md` with checkable items
2. Verify Plan: Check in before starting implementation
3. Track Progress: Mark items complete as you go
4. Explain Changes: High-level summary at each step
5. Document Results: Add review section to `tasks/todo.md`
6. Capture Lessons: Update `tasks/lessons.md` after corrections


## Core Principles

- **Simplicity First:** Make every change as simple as possible. Impact minimal code.
- **No Laziness:** Find root causes. No temporary fixes. Senior developer standards.
- **Minimal Impact:** Only touch what's necessary. No side effects with new bugs.

## Project Overview

Kegelkasse Kegelclub Manager — a full-stack 9-pin bowling club management PWA.
Manages clubs, evenings, games, penalties, member rosters, and treasury with offline-first capabilities and
German/English i18n.

## Development Commands

### Backend

```bash
# Start dev environment
docker compose -f docker-compose.dev.yml up -d

# Run migrations
docker compose exec app alembic upgrade head

# Seed initial superadmin
docker compose exec app python -m app.scripts.create_admin

# API docs available at http://localhost:8000/api/docs
```

### Frontend

```bash
cd frontend
npm install
npm run dev      # Dev server at :5173
npm run build    # TypeScript check + Vite production build
npm run preview  # Preview production build
```

### Environment

```bash
cp .env.example .env
# Required: DATABASE_URL, SECRET_KEY, FIRST_SUPERADMIN_EMAIL, FIRST_SUPERADMIN_PASSWORD
```

## Architecture

**Backend:** FastAPI + PostgreSQL + SQLAlchemy ORM + Alembic migrations. Runs inside Docker. API versioned at
`/api/v1/`. Serves the React SPA as static files in production (mounts build to `/assets`, fallback to `index.html`).

**Frontend:** React 19 + TypeScript + Vite. State via Zustand (persists `user` and `activeEveningId`). REST API calls
with JWT Bearer auth via `frontend/src/api/client.ts`. Real-time updates via 30s polling on the evening page. PWA with
service worker + IndexedDB for offline support.

**Auth flow:** JWT tokens, bcrypt passwords, invite-based registration (one-time tokens). Three roles: `superadmin`,
`admin`, `member`.

## Key Files

| File                             | Purpose                                                     |
|----------------------------------|-------------------------------------------------------------|
| `backend/app/main.py`            | FastAPI app setup, route registration, CORS, static serving |
| `backend/app/core/config.py`     | Environment settings                                        |
| `backend/app/core/security.py`   | JWT + password hashing                                      |
| `backend/app/api/deps.py`        | Auth dependencies, role checks                              |
| `backend/app/api/v1/evenings.py` | Main business logic (games, penalties, drinks, players)     |
| `backend/alembic/versions/`      | Numbered migrations (001–010 done)                          |
| `frontend/src/App.tsx`           | Router, header, nav, boot/auth flow                         |
| `frontend/src/store/app.ts`      | Zustand store, role helpers                                 |
| `frontend/src/api/client.ts`     | All API calls                                               |
| `frontend/src/types.ts`          | Shared TypeScript interfaces                                |
| `frontend/src/i18n/de.ts`        | German translations (source of truth for keys)              |
| `frontend/src/i18n/en.ts`        | English translations (must stay in sync with de.ts)         |
| `frontend/vite.config.ts`        | Vite + PWA config, API proxy                                |

## Data Model

- `Club` → `RegularMembers`, `PenaltyTypes`, `GameTemplates`, `Evenings`, `ClubTeams`
- `Evening` → `EveningPlayers`, `Teams`, `Games`, `PenaltyLog`, `DrinkRounds`
- `EveningPlayer.is_king` — set on the Eröffnungsspiel winner (one per evening)
- `Game.status` — `open` → `running` → `finished`
- `Game.started_at` / `finished_at` — timestamps for game timing
- `PenaltyLog.game_id` — FK to Game for auto-created loser penalties
- `PenaltyLog.regular_member_id` — FK to RegularMember for absence entries
- `PenaltyLog.unit_amount` — default_amount frozen at log time (retroactive-safe for count mode)
- `ClubSettings.extra` JSON — stores `bg_color`, `guest_penalty_cap`
- Soft deletes via `is_deleted` flag on `Game` and `PenaltyLog`
- `Evening.is_closed` archives to history
- `Evening.ended_at` — set when the evening is closed (editable in the close-confirm UI, defaults to now if unset); persists across reopen/re-close unless explicitly overridden; absence penalties (`_do_calculate_absence_penalties`) use it as their `client_timestamp` instead of the calculation time

## Coding Conventions

- **i18n:** All UI strings via `useT()`. Add keys to `de.ts` first, then `en.ts`. Keys follow `scope.sub.key` naming.
- **Admin guards:** `require_club_admin` in backend deps, `isAdmin(user)` check or `<AdminGuard>` in frontend.
- **Migrations:** One file per DB change, numbered sequentially (`NNN_description.py`). Never modify existing
  migrations.
- **API client:** All fetch calls in `frontend/src/api/client.ts`. Return types are TypeScript interfaces from
  `types.ts`.
- **Sheets:** Bottom-sheet dialogs via `<Sheet open onClose title onSubmit>`. Escape key closes automatically.
- **Toasts:** Errors via `showToast(message)`.
- **Store:** Zustand store in `store/app.ts`. Only persists `user` and `activeEveningId`. Other data (penaltyTypes,
  etc.) is populated at boot and not persisted.
- **Game loser penalties:** Always created via `finish_game` endpoint (not `add_game`). Identified by
  `penalty_log.game_id`. On re-edit: old penalties deleted, new ones created.
- **Docs & README & CLAUDE.md:** Whenever a user-facing feature is added or changed, update the relevant page(s) in
  `docs/docs/`, the feature catalog in `README.md`, **and** the Feature Roadmap table in `CLAUDE.md` (status, notes).
  Keep all three in sync with the implementation. Do this **before committing** — never skip it, even for small changes.
- **Versioning:** A single semantic version (`MAJOR.MINOR.PATCH`) for the whole app lives in `frontend/package.json`
  (field `"version"`). It is injected at build time as `__APP_VERSION__` via `vite.config.ts` and displayed in the
  ProfileSheet footer. Bump the version in `package.json` with every release or significant feature:
  `MAJOR` for breaking changes, `MINOR` for new features, `PATCH` for bug-fixes. Never edit the displayed version
  elsewhere — always update `frontend/package.json` as the single source of truth.
- **Linting & build:** CI (`frontend-build.yml`, `backend-build.yml`) already runs `npm run lint`, `npm run build`,
  and `poetry run ruff check .` on every push and PR, and these pipelines finish in just a few minutes. Default to
  letting CI be the gate for **every** change, not just small ones: skip running the full local lint/build/test
  suite as a pre-push ritual, push, `subscribe_pr_activity` on the PR, and watch the check runs — that's the fast,
  low-check-in-time path, and re-running everything locally on top of it is wasted time. Only run something locally
  when actively iterating on a change it would likely catch (touching many files, renaming types, reworking
  imports) or a single file you're editing repeatedly — not as a blanket "verify everything before I push" step.
  Fix any errors before the PR is mergeable regardless of whether you caught them locally or via CI.
- **Parallel agents → rebase before push:** Multiple agents/sessions often work on separate branches off `main`
  concurrently, so a branch can fall behind between when work started and when it's ready to push, and PRs
  frequently need a rebase as a result. Before pushing, `git fetch origin main` and check whether the branch has
  diverged (`git log HEAD..origin/main --oneline`); if it has, rebase onto the latest `main` and resolve conflicts
  rather than force-discarding either side's changes. Check again after CI finishes on the PR — another PR can merge
  into `main` while your CI was running — and re-rebase (and re-push) if `main` moved before treating the PR as
  mergeable.
- **Backend dependencies:** Whenever `backend/pyproject.toml` is changed (adding, removing, or updating a package),
  immediately run `cd backend && poetry lock` to regenerate `poetry.lock` and commit both files together.
- **Dependency freshness:** Always keep dependencies at their latest compatible versions. When Dependabot opens PRs,
  handle them promptly — merge safe patch/minor bumps directly, and perform major-version migrations immediately rather
  than deferring them. Do not let major-version upgrades accumulate. Known blocking constraints (document them inline):
  - `eslint-plugin-react-hooks@7.x` caps ESLint at `^9` → unblocks when a new release adds `^10` support.
  - `typescript` capped at `^6.x` → `@typescript-eslint` (`typescript-estree`) declares `peerDependencies.typescript: ">=4.8.4 <6.1.0"` and crashes (`Cannot read properties of undefined (reading 'Cjs')`) against TypeScript 7 (the Corsa/native rewrite); unblocks when `@typescript-eslint` ships TS7 support.
- **Design consistency:** Apply the established design system everywhere and immediately — tabs, sheets, top-level
  page elements, dialogs, and any new components. Never leave new UI without consistent styling.
- **Display names:** Always show the Kegelname (nickname) as the primary display name for members. Use
  `member.nickname || member.name` everywhere a member name is shown — including dropdowns, select options, filter
  chips, list cards, alerts, and any other UI that references a member. In components linked to a `User`, look up the
  linked `RegularMember` via `user.regular_member_id` in the `regularMembers` store to get the nickname. Never display
  `member.name` alone when a nickname exists.
- **"Ich" label:** In every list or card that shows members/players, add a small amber `Ich` badge
  (`<span className="text-[9px] text-kce-amber font-bold">Ich</span>`) next to the current user's entry. Compare
  `regular_member_id === user?.regular_member_id` (or `user.id` for `AppUser` lists). Exception: pure rankings
  (e.g. annual penalty ranking) keep their sorted order but still show the `Ich` badge.
- **Current user first:** In non-ranking lists (players, members, accounts, rosters), always sort the current user's
  entry to the top. Use `.sort((a, b) => { if (a.xxx === myId) return -1; if (b.xxx === myId) return 1; return 0 })`.
- **Sync mutations:** After any mutation (create, update, delete), immediately trigger a re-fetch of all affected
  lists/data so other clients (and the current client) see the updated state without manual refresh. Use the existing
  polling mechanism or invalidate relevant queries right after the API call resolves.
- **UI invalidation:** Whenever a data entry changes, always invalidate and reload the affected list(s) in the UI.
  Never rely on local optimistic state alone — always confirm with a fresh server response.
- **Data dependency invalidation:** When creating data X that other queries depend on, always invalidate those
  dependent queries immediately. Examples: creating a `PaymentRequest` → invalidate `['payment-requests']` and
  `['my-payment-requests']`; confirming a request → also invalidate `['my-balance']` and `['my-payment-requests']` so
  the member's profile view stays in sync without a manual refresh.
- **Testing policy:** Every new feature **must** ship with tests — no exceptions, no deferred "add tests later".
  - **Backend:** Every new API endpoint gets a pytest test in `backend/tests/test_<module>.py`. Cover the happy path,
    auth/role guards (401 unauthenticated, 403 wrong role), and the main error cases (404, 400). Use the existing
    conftest fixtures (`db`, `club`, `user`, `auth_headers`) and add an autouse `cleanup` fixture that deletes all rows
    created by that test module in correct FK order (children before parents) and depends on `club` so it runs before
    club teardown.
  - **Frontend:** Every new utility function, store action, or pure logic module gets a Vitest test in a `__tests__/`
    sibling directory. Stubs/mocks go in `beforeEach`; always restore with `vi.unstubAllGlobals()` or `afterEach`.
    Page-level UI is lower priority but must be added when feasible.
  - **Run tests before committing:** CI (`backend-tests.yml`, `frontend-tests.yml`) already runs the full pytest and
    Vitest suites on every push and PR in just a few minutes, so don't re-run the entire suite locally as a pre-push
    ritual — checking the PR's pipeline is the verification step, not a local checklist item, and this applies
    broadly, not only to minor changes. At most, run the test file(s) touching what you changed (e.g.
    `poetry run pytest tests/test_<module>.py` or `npx vitest run <file>`) before committing if you want faster
    local signal while iterating, then push and watch CI (`subscribe_pr_activity` on the PR) rather than also
    running the full suite locally afterwards. Run the full suite locally only when you have concrete reason to
    suspect cross-file breakage the targeted tests wouldn't catch — not as a default habit. Never knowingly push
    code you expect to fail CI — if in doubt about a specific risk, test that risk locally rather than defaulting to
    a full run.

## Deployment

Push to Git → Coolify builds Docker Compose. The `docker/entrypoint.sh` auto-runs migrations and admin seed on container
start. No manual migration steps needed in production.

## Feature Roadmap

Status: ✅ Done · 🚧 In Progress · ⬜ Planned

| #  | Feature                            | Status | Notes                                                                                                 |
|----|------------------------------------|--------|-------------------------------------------------------------------------------------------------------|
| 1  | **Spiele**                         | ✅      | Status-Flow (open→running→finished), König-Flag, Verlierer-Strafen; beim Neuberechnen der Verliererstrafen (Bearbeiten eines beendeten Spiels) verwenden die neu erstellten Strafeneinträge den `finished_at`-Zeitstempel des Spiels statt des Bearbeitungszeitpunkts. **Start-/Endzeit nachträglich korrigieren**: 🕐-Button (Admin, laufende/beendete Spiele) öffnet eigenes Sheet mit `datetime-local`-Feldern für `started_at`/`finished_at` — zum Nachtragen, wenn Start/Ende am Abend vergessen wurde, oder zur Korrektur falscher Zeiten. `PATCH /evening/{eid}/games/{gid}` akzeptiert optionale ISO-`started_at`/`finished_at`-Felder (getrennt von den übrigen Metadaten geparst/validiert, bevor irgendetwas am Game-Objekt gesetzt wird — verhindert eine dreckige SQLAlchemy-Session bei abgelehnter Validierung); admin-only (403 für normale Mitglieder), 400 wenn `finished_at` vor `started_at` läge. **Verlierer-Strafen werden beim Retiming mit-verschoben:** vorher übernahmen die Auto-Strafen eines beendeten Spiels den neuen `finished_at`-Zeitstempel nur, wenn zugleich `loser_penalty`/`per_point_penalty` geändert wurde (da nur das ein `_apply_game_penalties`-Neuerstellen auslöste) — eine reine Zeitkorrektur ließ die bestehenden `PenaltyLog`-Einträge auf dem alten Zeitstempel stehen. Jetzt aktualisiert `update_game` bei reiner `finished_at`-Änderung (ohne Betragsänderung) den `client_timestamp` der bestehenden Auto-Strafen direkt in-place (kein Löschen/Neuanlegen, IDs bleiben stabil). Nebenbei behobener Randfall: der Vergleich `finished_at < started_at` verglich das bereits gespeicherte `started_at`/`finished_at` des Spiels ungeprüft gegen neu geparste, stets tz-aware Werte — auf Backends, die tz-aware Spalten beim Round-Trip als naive Datetimes zurückgeben, führte das zu einem `TypeError`; beide Bestandswerte werden jetzt vor dem Vergleich auf UTC normalisiert. pytest: 3 + 1 neue Tests (7 gesamt für `TestUpdateGame`). **Verlierer-Strafen-Vorschau jetzt immer sichtbar:** Der Beenden-Dialog zeigte die €-Vorschau pro Verlierer (`game.perPointPreview`-Block) bisher nur, wenn die Vorlage eine punktebasierte Zusatzstrafe (`per_point_penalty > 0`) hatte — bei reinen Flatrate-Verliererstrafen fehlte die Vorschau komplett. Die Bedingung in `GamesPage.tsx` (`(finishTarget.per_point_penalty ?? 0) > 0 && winnerRef`) ist jetzt auf `winnerRef` allein reduziert; der Punktebezug (`+X €/Pkt`) in der Kopfzeile wird nur noch angezeigt, wenn tatsächlich eine Zusatzstrafe existiert. Dieselbe Vorschau (vorher gar nicht vorhanden) ist jetzt auch im Beenden-Panel der Tablet-Schnellerfassung (`TabletQuickEntryPage.tsx`) eingebaut, basierend auf `activeGame.loser_penalty`/`per_point_penalty` statt eines editierbaren Betragsfelds (die Tablet-Ansicht übernimmt den Spiel-Standardbetrag unverändert). Vitest: 2 neue Tests.                                    |
| 2  | **Strafen-Log Anreicherung**       | ✅      | Spiel-Kontext-Label, Spieler-Filter-Chips                                                             |
| 3  | **Kasse**                          | ✅      | Ranking, Spiele/Getränke, Text-Export (Share/Copy); **Saldo-Verlauf-Graph** (📈 Verlauf) in der Übersicht: SVG-Step-Chart mit Kasse/Mitglied-Umschalter (`ModeToggle`) — Kasse = Vereins-Cashflow (Zahlungen+Ausgaben), Mitglied = Pille-Liste aller Mitglieder (eigenes Konto zuerst, Ich-Badge); Monat/Jahr/Alle-Ansichten mit durchgehender kumulativer Basislinie (Blättern setzt Kurve nie auf Null zurück), bei „Alle" horizontal scrollbar mit fixierter Y-Achse; zwei parallele Linien „Tatsächlich" (reale Buchungen) und „Inkl. Schulden"/„Inkl. Strafen" (virtueller Saldo inkl. offener Schulden zum jeweiligen Zeitpunkt via `GET /club/treasury-debt-timeline`, bzw. inkl. verbuchter Strafen via `GET /club/member-penalties/{mid}`); klickbare Punkte zeigen Datum/Art/Betrag der zugrunde liegenden Buchung sowie den daraus resultierenden kumulativen Saldo beider Linien, Schulden-Stützpunkte sind nicht antippbar; „Tatsächlich"-Linie nutzt `var(--kce-cream)` statt eines hartkodierten Hex-Werts, damit sie unter jedem per-Club-Theme (Feature #31) sichtbar bleibt; „Inkl. Schulden/Strafen"-Linie (Dot-Highlight + Legende) nutzt `var(--kce-primary)` statt eines nicht existenten `var(--kce-amber)` (nur `--color-kce-amber` existiert als Tailwind-Theme-Token, keine literale `:root`-Variable — der Stroke resolvte zu `none` und war unsichtbar); x-Achsen-Datumslabels sind pro Kalendertag dedupliziert (`labelOwnerByDate`/`labelOwnerIndices` in `BalanceHistoryChart`) — vorher konnte `labelEvery` (rein index-basierte Sparsity, ohne Datumsbezug) bei mehreren Buchungen am selben Tag dasselbe „DD.MM."-Label doppelt nebeneinander rendern, inkl. Sonderfall ausgewählter Punkt vs. bereits sichtbares Label am gleichen Tag; `lib/balanceHistory.ts` (pure logic, 24 Vitest-Tests). **Audit-Trail & Idempotenz** (Migration 047): `MemberPayment`/`ClubExpense` löschen ist jetzt Soft-Delete (`is_deleted`/`deleted_at`/`deleted_by`/`delete_reason`, analog zu `PenaltyLog.is_deleted`) statt Hard-Delete — alle Salden-Endpoints (`member-balances`, `guest-balances`, `my-balance`, `treasury-debt-timeline`, `remind-debtors`, Saisonabschluss-`_compute_balances`) filtern `is_deleted == False`; Lösch-Sheet in der TreasuryPage hat ein optionales Grund-Feld; Mitglied bekommt Push wenn eine seiner Zahlungen storniert wird; `POST /club/member-payments` validiert jetzt `amount != 0` (vorher ungeprüft); client-generierter `idempotency_key` (UUID) auf Zahlungs-/Ausgaben-Erstellung verhindert Doppelbuchung bei Doppelklick/Request-Retry (Server gibt bei bekanntem Key den existierenden Datensatz zurück statt einen neuen zu erzeugen); `transfer_group_id` verknüpft zusammengehörige Buchungspaare (Gast-Kosten-Übertragung, Saisonabschluss-Übertrag) statt sich auf Notiztext-Matching zu verlassen (Saisonabschluss-Wiedereröffnung nutzt weiterhin Notiztext als Fallback für vor Migration 047 geschlossene Saisons); `PaymentRequest` confirm/reject nutzt `with_for_update()` gegen doppeltes Bestätigen bei gleichzeitigen Admin-Klicks. **Buchungen bearbeiten** (Migration 048): `PATCH /club/member-payments/{pid}` (Betrag, Notiz) und `PATCH /club/expenses/{eid}` (Betrag, Beschreibung, Datum) — admin-only, `updated_at`/`updated_by`-Audit-Spalten auf beiden Tabellen; ✏️-Bearbeiten-Knopf mit Edit-Sheet (Richtung/Betrag/Notiz, bei Ausgaben Datum) im Kassenbuch-Tab und in der Zahlungs-Historie der Konten; bearbeitete Buchungen tragen einen ✏️-Marker neben dem Datum; Mitglied bekommt Push bei Betragsänderung einer seiner Zahlungen; pytest: 17 Tests, Vitest: 2 API-Client- + 5 TreasuryPage-Tests. **Verständlichkeits-Überarbeitung der Übersicht:** „Mein Konto"-Karte ganz oben (eigener offener Betrag/Guthaben/Alles-bezahlt-Status, Strafen-vs-Bezahlt-Summen, Fortschrittsbalken bezahlter Strafen-Anteil, PayPal-Zahlen/Melden-Aktionen — aus der eigenen Schuldner-Zeile hierher verschoben, Konten-Tab-Kopie bleibt); Kassenstand-Hero mit explizitem Geldfluss-Breakdown (⬆ Eingezahlt Mitglieder+Gäste, ⬇ Ausgaben netto, 🔴 Noch offen inkl. Gäste, → Kassenstand wenn alle zahlen); aufklappbare „Wie funktioniert die Kasse?"-Erklärkarte (4 Sätze zum Modell Strafen→Einzahlung→Kassenstand); `PaidShareBar`-Fortschrittsbalken (bezahlter Anteil der Strafen) auf allen Schuldner-/Guthaben-/Konten-/Gäste-Zeilen; hartkodierte „Strafen:/Bezahlt:"-Labels durch i18n-Keys ersetzt; Layout-Bug behoben (Einnahmen&Ausgaben-Karte war drittes Kind im 2-Spalten-Grid — im Hero-Breakdown aufgegangen); reine Ableitung in `lib/treasurySummary.ts` (`treasurySummary` + `paidShare`, 12 Vitest-Tests). **Ausgaben/Sonstige-Einnahmen-Split & aufklappbare Buchungen:** die Hero-Zeile „Ausgaben (netto)" verrechnete positive Ausgaben- und negative Einnahme-Einträge (`ClubExpense.amount`) zu einer Netto-Zahl und drehte bei negativem Netto Vorzeichen/Farbe um (`+965,20 €` grün trotz Label „Ausgaben") — verwirrend, weil nicht ersichtlich war, dass hinter der Zahl eigentlich eine Netto-Einnahme steckt. `treasurySummary()` liefert jetzt zusätzlich `expensesGross` (nur positive Einträge) und `otherIncome` (Betrag der negativen Einträge) statt nur des genetteten `expensesNet`; die Hero-Karte zeigt **Ausgaben** und **Sonstige Einnahmen** (nur sichtbar wenn > 0) als getrennte Zeilen statt einer vorzeichen-geflippten. Jede Hero-Zeile (Eingezahlt/Ausgaben/Sonstige Einnahmen/Offen) ist jetzt per Klick aufklappbar (`FlowRow`-Komponente, lokaler `flowDetail`-State) und zeigt die zugrunde liegenden Einzelbuchungen (Mitglied/Gast + Betrag bzw. Ausgaben-Beschreibung + Datum + Betrag) — ohne in den Kassenbuch-Tab wechseln zu müssen; leere Aufschlüsselung zeigt `treasury.flow.noEntries`. `expensesNet` (= `expensesGross - otherIncome`) bleibt unverändert für Kassenstand-Berechnung und den Kassenbuch-Tab („Einnahmen & Ausgaben (Saldo)"). Vitest: 3 neue treasurySummary-Tests (Split-Werte, Rundung). **Konten-Tab: Gesamtsummen & Anteil-pro-Spieler-Chart:** Guthaben (Credit) eines Mitglieds zählt zwar zum Kassenstand (reales, eingezahltes Geld), ist aber der Kasse im übertragenen Sinn „geschuldet" — es wird automatisch mit künftigen Strafen verrechnet (bzw. bei Austritt ausgezahlt, Feature #45) statt frei verfügbares Vereinsgeld zu sein. Der Konten-Tab zeigte bisher nur Einzelkonten ohne Gesamtübersicht; jetzt zwei Kennzahl-Kacheln oben („Offen gesamt" = `totalOutstanding`, „Bezahlt gesamt" = neuer `totalPaidMembers` = Summe aller `payments_total`) — ist in „Bezahlt gesamt" Guthaben enthalten, weist eine Zeile darauf hin, dass dieser Anteil der Kasse geschuldet ist (`treasury.accounts.creditOwed`). Darunter aufklappbare **📊 Anteil pro Spieler**-Karte (collapsed by default, gleiches Muster wie „Wie funktioniert die Kasse?", lokaler `showAccountsChart`-State) — pro Mitglied ein horizontaler Balken, bezahlter (grün) vs. offener (rot) Anteil der `penalty_total`, Segmente durch 2px-Lücke getrennt, skaliert auf `maxAccountPenalty` (höchste `penalty_total` aller Konten) statt auf das eigene Total, damit Beträge zwischen Mitgliedern vergleichbar sind (anders als die bestehende `PaidShareBar`, die pro Zeile auf 100 % des eigenen Strafen-Totals skaliert); Legende (grüner/roter Punkt + Text) statt Farbe allein. Bewusst collapsed-by-default statt immer sichtbar: eine Version ohne Aufklapp-Zustand duplizierte Mitgliedsnamen im DOM (Konten-Liste + Chart gleichzeitig sichtbar) und brach 13 bestehende Klick-Tests, die per `getByText(name)` das Konto-Zeilen-Element eindeutig referenzierten. pytest: unverändert (rein Frontend); Vitest: 3 neue TreasuryPage-Tests (Gesamtsummen, Guthaben-Hinweis, Chart bleibt bis zum Antippen eingeklappt) **Datum auf Mitglieder-Zahlungen editierbar** (Migration 050): `MemberPayment` bekommt dieselbe optionale `date`-Spalte wie `ClubExpense` (Nachtragen/Backdaten); `POST /club/member-payments` und `PATCH /club/member-payments/{pid}` akzeptieren jetzt beide ein optionales ISO-`date`-Feld (leerer String löscht das Datum beim PATCH), analog zu den Ausgaben-Endpoints; Kassenbuch-Bearbeiten-Sheet und „+ Buchung"-Anlegen-Sheet zeigen das Datumsfeld jetzt für beide Buchungsarten (vorher nur für Vereinsausgaben) — vorher ließ sich das Datum einer bereits erfassten Mitglieder-Zahlung gar nicht korrigieren; Sortierung im Kassenbuch (`mergedBookings`) sowie die Saldo-Verlauf-Chart-Events (`clubEventsFromBookings`/`memberPaymentEvents` in `lib/balanceHistory.ts`) nutzen jetzt ebenfalls `date ?? created_at` statt für Zahlungen ausschließlich `created_at`; pytest: 5 neue Tests, Vitest: bestehende Buchungs-Tests angepasst. **Verlauf-Graph: Y-Achse, Linien-Beschriftung & X-Achsen-Clustering (Bug-Fixes):** Die €-Beschriftung der Y-Achse existierte bisher nur als separates fixiertes `<svg>` neben dem scrollbaren Chart in der „Alle"-Ansicht (`isAll`-Zweig) — Monat/Jahr rendern das Haupt-`<svg>` direkt ohne dieses Wrapper-Element und zeigten deshalb nur Gitterlinien ohne Zahlen; das Haupt-`<svg>` selbst zeichnet jetzt zusätzlich `<text>`-Ticks (`!isAll`-Zweig), sodass Monat/Jahr dieselbe Y-Achsen-Beschriftung wie „Alle" bekommen, ohne sie in „Alle" zu duplizieren. Die Linien-Legende „Tatsächlich"/„Inkl. Strafen" in der Mitglied-Ansicht war irreführend (dieselbe „Tatsächlich"-Beschriftung wie in der Kasse-Ansicht, obwohl sie dort etwas anderes bedeutet, und „Inkl. Strafen" klang additiv trotz sinkender Linie) — neuer scope-spezifischer Label-Satz: Mitglied zeigt „Eingezahlt" (Summe der Zahlungen) vs. „Saldo" (Kontostand nach Strafen) statt „Tatsächlich"/„Inkl. Strafen" (neuer i18n-Key `treasury.history.actualMember`, `virtualMember` umbenannt); Kasse-Ansicht bleibt bei „Tatsächlich"/„Inkl. Schulden". X-Achse in Monat/Jahr clustert Punkte jetzt auf diskrete, gleichmäßig verteilte Buckets statt eine zeitproportionale Skala zu nutzen — Monat: ein Bucket pro Kalendertag mit Aktivität (= Kegelabend), Jahr: ein Bucket pro Kalendermonat (bis zu 12 Stützpunkte) — vorher verteilten sich wenige Buchungen in einem sonst ruhigen Zeitraum zeitproportional stark ungleichmäßig; neue reine `bucketStart(ts, granularity)`-Funktion in `lib/balanceHistory.ts` (5 neue Vitest-Tests), `xS()`/`buildPath()` in `BalanceHistoryChart` nutzen bei Monat/Jahr Bucket-Index statt Timestamp für die X-Position, „Alle" bleibt unverändert zeitproportional; X-Achsen-Beschriftung nutzt jetzt durchgängig das vorher ungenutzte `formatTick()` (Monat: „DD.MM.", Jahr: Kurzmonat) statt eines hartkodierten `DD.MM.`-Formats auch in der Jahres-Ansicht. **„+ N ausgeglichen" aufklappbar:** die Zeile unter den Schuldner-/Guthaben-Listen war reiner Text ohne Möglichkeit zu sehen, wer gemeint ist (nur im Konten-Tab pro Zeile sichtbar) — jetzt ein Toggle (`showSettled`-State), der eine Pille-Liste der exakt ausgeglichenen Mitglieder aufklappt (eigenes Konto zuerst, Ich-Badge), analog zum bereits vorhandenen `FlowRow`-Aufklapp-Muster. Vitest: bestehende Suite (1846 Tests) grün, `npm run build` clean. **Nach Spielern filtern (Übersicht):** aufklappbare **🔍 Nach Spielern filtern**-Karte (collapsed by default, gleiches Muster wie „Wie funktioniert die Kasse?", lokaler `showBalanceFilter`/`balanceFilterIds`/`balanceFilterMode`-State) direkt über den „Offen"/„Guthaben"-Kacheln — Mitglieder-Pillen (Multi-Select, eigenes Konto zuerst, Ich-Badge) plus `ModeToggle` (erscheint erst sobald ≥1 Mitglied ausgewählt ist) mit zwei generischen Modi: **Ohne Auswahl** (`exclude`, Default) schreibt die offenen Strafen der Auswahl ab (bereits Bezahltes bleibt unverändert) — praktisch z. B. für Spieler, die den Verein bald verlassen könnten, aber nicht darauf beschränkt; **Nur Auswahl** (`only`) filtert Summen und Listen auf exakt die ausgewählte Teilmenge, ganz ohne Abschreibungslogik. Kein separates Vergleichs-Panel (bewusst kein „Aktuell vs. danach"): der Filter speist stattdessen direkt die bereits bestehenden „Offen"/„Guthaben"-Kacheln und -Listen über eine neue `effectiveBalances`-Ableitung — `filteredTotalOutstanding`/`filteredTotalSurplus`/`filteredDebtors`/`filteredCredits`/`filteredExactlySettled` ersetzen dort die bisherigen `balances`-basierten Werte; der Konten-Tab und die Kassenstand-Hero (`summary`) bleiben unverändert auf den ungefilterten `balances`, da Vereinsbargeld nicht sinnvoll auf eine Spieler-Teilmenge heruntergebrochen werden kann. Reine Funktion `writeOffOutstandingDebt` in `lib/treasurySummary.ts` (generisch benannt, kein „Austritt"-Bezug im Code) — Ergebnis läuft direkt wieder durch `treasurySummary()` bzw. wird bei „Nur Auswahl" einfach auf die Teilmenge gefiltert (kein Sonder-Rechenpfad). Vitest: treasurySummary-Tests für `writeOffOutstandingDebt` + 5 TreasuryPage-Tests (Ein-/Ausklappen, Modus-Umschalter erscheint erst nach Auswahl, Ohne-Auswahl schreibt Schuld ab, Nur-Auswahl filtert auf Teilmenge, Abwählen stellt Gesamtsumme wieder her). **Export hinter Button/Sheet:** die Jahr-/Format-Auswahl für den Bericht-Export stand bisher als eigene Zeile fest über den Tabs (bei jedem Tab-Wechsel sichtbar) — zusätzliches visuelles Rauschen auf einer bereits vollen Übersichtsseite. Jetzt ein kompakter „📊 Export"-Button (admin-only) in der Kopfzeile neben der Kasse-Überschrift, der ein eigenes Sheet mit Jahr-/Format-Auswahl + Download-Button öffnet; Export-States (`exportYear`/`exportFormat`/`exporting`) unverändert. Vitest: bestehende Export-Tests auf den Sheet-Flow umgestellt (94 Tests grün), `npm run build` clean. Filter jetzt global statt nur auf „Offen & Guthaben" beschränkt:** vorher blieben die Kassenstand-Hero (`summary`) und deren Geldfluss-Zeilen (Eingezahlt/Ausgaben/Sonstige Einnahmen/Offen/Projiziert) sowie die „Tatsächlich"-Linie des 📈 Verlauf-Graphs im 🏛️ Kasse-Modus unverändert auf den ungefilterten `balances` — das widersprach der Erwartung, dass eine aktive Spieler-Auswahl sich überall in der Übersicht auswirkt. `effectiveBalances` wird jetzt vor `summary` berechnet und dort statt der rohen `balances` übergeben (`summary = treasurySummary(effectiveBalances, guestBalances, expenses)`); die Hero-Zeilen-Aufschlüsselungen (`paidInBreakdown`/`outstandingBreakdown`, Basis `allBalancesForFlow`) nutzen ebenfalls `effectiveBalances` statt `balances`. Für den Verlauf-Graph filtert ein neuer `filteredClubPayments`-Ausdruck `allPayments` im „Nur Auswahl"-Modus auf die ausgewählten `regular_member_id`s (Gäste, nie Teil der Auswahl, passieren über einen `guestIds`-Set immer unverändert durch); „Ohne Auswahl" lässt die Zahlungen unverändert, da bereits real eingezahltes Geld nicht rückwirkend verschwindet. Die „Inkl. Schulden"-Überlagerungslinie (`debtEventsFromTimeline`) bleibt bewusst clubweit unverändert — sie stammt aus einem einzigen, nicht pro Mitglied aufschlüsselbaren Backend-Zeitstrahl. Konten-Tab bleibt unverändert die einzige Ausnahme (immer vollständige Vereinsübersicht). Die **„Wie funktioniert die Kasse?"**-Erklärkarte ist keine eigene Karte mehr, sondern eine unauffällige, kleingedruckte aufklappbare Zeile am unteren Rand der Kassenstand-Hero. **Verlauf-Graph: geclusterte Punkte jetzt klickbar:** mehrere Buchungen im selben X-Achsen-Bucket (z. B. zwei Zahlungen am selben Kegelabend in der Monats-Ansicht) rendersen vorher als übereinanderliegende Kreise, von denen nur der zuletzt gezeichnete antippbar war — die übrigen waren de facto nicht klickbar. Neue reine Funktion `clusterPoints(points, granularity)` in `lib/balanceHistory.ts` gruppiert Punkte nach Bucket **und** Kurve (Ist- vs. Überlagerungslinie) zu `PointCluster`-Objekten; `BalanceHistoryChart` rendert einen Marker pro Cluster (mit „×N"-Beschriftung bei mehreren Einträgen) statt einen Kreis pro Punkt, und die Detailansicht listet beim Antippen alle Einträge des Clusters statt nur eines einzelnen Ereignisses. Vitest: 5 neue `clusterPoints`-Tests + 4 neue TreasuryPage-Tests (globaler Filter auf Hero-Zeile, Cluster-Kollaps, Cluster-Klick zeigt alle Einträge). **Filter-Karte nach oben verschoben:** die **🔍 Nach Spielern filtern**-Karte stand bisher unterhalb von Kassenstand-Hero und Verlauf-Graph, obwohl sie beide seit „Filter jetzt global" beeinflusst — Ursache (Filter) erschien optisch nach der Wirkung (gefilterte Zahlen). Reine JSX-Verschiebung ohne Logikänderung: die Karte sitzt jetzt direkt unter „Mein Konto" (das der Filter nie betrifft — immer das eigene, ungefilterte Konto) und vor der Kassenstand-Hero; `showBalanceFilter`/`balanceFilterIds`/`balanceFilterMode`/`effectiveBalances` bleiben unverändert im Komponenten-Body deklariert. Neue Reihenfolge: Mein Konto → Nach Spielern filtern → Kassenstand-Hero → Verlauf-Graph → Offen & Guthaben. **Verlauf-Graph: Zugänglichkeit der Punkte (Bug-Fixes):** (1) **„Inkl. Schulden"-Stützpunkte jetzt antippbar** — vorher galt `kind === 'debt'` in `isAttributable` als nicht-attribuierbar und gatete via `attributable = cluster.points.some(isAttributable)` sowohl `role="button"`/`tabIndex`/`onClick` als auch die unsichtbare Tap-Fläche komplett aus, sodass reine Schulden-Overlay-Cluster gar nicht erreichbar waren. Jetzt ist **jeder** Cluster klickbar; die Detailansicht eines Schulden-Punkts zeigt die Veränderung der offenen Gesamtschuld (`delta` = Diff zum Vorcheckpoint) plus den resultierenden virtuellen Saldo. Neuer i18n-Key `treasury.history.kindDebt` („Offene Schulden"/„Outstanding debt") füllt `KIND_LABEL.debt` (vorher leerer String) und den Aria-/Detail-Text; `isAttributable` bleibt exportiert (Lib-Tests) und beschreibt weiter „mappt auf eine einzelne Buchung", gatet aber keine Klickbarkeit mehr (Import in `TreasuryPage` entfernt). (2) **Alle Punkte leichter treffbar:** die transparente Tap-Fläche (`<circle r>`) wird jetzt für jeden Cluster gerendert (vorher nur attribuierbare) und von 10 auf 13px vergrößert. (3) **Fehlende X-Achsen-Labels pro Bucket:** die Label-Auswahl lief über `labelEvery` (rein Punkt-Index-basiert) + Datums-Dedup — bei mehreren Buchungen im selben Bucket konnte eine ganze Bucket-Spalte ohne Datum bleiben. In Monat/Jahr wird jetzt **pro Bucket** ein Repräsentant-Index gewählt (bevorzugt ein selektierter Punkt, damit das aktive Bucket-Label hervorgehoben rendert) und nur bei >8 Buckets gleichmäßig ausgedünnt (`bucketEvery`); „Alle" behält die Index-Sampling-+Dedup-Logik (kontinuierliche Zeitachse). (4) **Detailliste scrollbar:** der `history-detail`-Container bekommt `max-h-40 overflow-y-auto`, damit ein Bucket mit sehr vielen Einträgen (v. a. dichte Schulden-Checkpoints) die Seite nicht sprengt. Vitest: 2 neue TreasuryPage-Tests (Schulden-Overlay-Marker klickbar + Detail zeigt Schulden-Art & Deltas; Detailliste scrollbar bei vielen Einträgen), volle Suite grün, `npm run build` clean. **Filter-Follow-up (Bedienbarkeit + Optionen):** (1) Zähl-Badge neben dem Titel (`balance-filter-active`, sichtbar auch eingeklappt) zeigt die Anzahl ausgewählter Mitglieder; (2) **Zurücksetzen**-Knopf (`balance-filter-clear`) in der Kopfzeile leert `balanceFilterIds` und setzt alle Optionen auf Standard (`clearBalanceFilter`); (3) der als redundant empfundene `exclude`/`only`-`ModeToggle` (`balanceFilterMode`-State entfernt) ist ersetzt durch unabhängige Checkbox-Optionen: **Nur Auswahl anzeigen** (`balanceOnlySelected`, reine Ansichts-Einschränkung wie zuvor `only`) und — im Simulations-Modus „Auswahl verlässt den Verein" — **Offene Strafen abschreiben** (`balanceWriteOffDebt`, Default an, = altes `exclude`-`writeOffOutstandingDebt`), **Eingezahltes abziehen** (`balanceRefundPaid`, neue reine Funktion `refundPaidIn` nullt `payments_total` der Auswahl → senkt `paidIn`/`cashOnHand`) und **Anteil verrechnen** (`balanceSettleShare`, neue reine Funktion `shareSettlement(otherIncome, expensesGross, memberCount, selectedCount)` = `selectedCount × (otherIncome − expensesGross) / n`, n = `balances.length`). Die Anteils-Auszahlung `shareOut` wird auf `cashOnHand`/`projectedCash` verrechnet und als eigene Geldfluss-Zeile **Anteil Auswahl** (`flow-amount-share`, `treasury.flow.shareSettlement`) angezeigt; die Simulations-Checkboxen sind deaktiviert, solange „Nur Auswahl anzeigen" aktiv ist. `refundPaid`/`settleShare` wirken bewusst nicht auf die Schuldner-/Guthaben-Listen (reine Kassenstand-Konzepte); die „Inkl. Schulden"-Überlagerungslinie bleibt clubweit. Vitest: neue `refundPaidIn`- + `shareSettlement`-Tests in `treasurySummary.test.ts` sowie überarbeitete TreasuryPage-Filter-Tests (Badge/Clear, Optionen erst nach Auswahl, Write-off-Toggle, Refund senkt paidIn, Share-Zeile). **Verlauf-Graph: „Alle"-Bucketing, Mitglied-Drei-Linien & spielerbezogene Schulden-Punkte:** (1) **„Alle" clustert jetzt pro Kalendertag** statt zeitproportional — `bucketStart(ts, 'all')` bucketet wie die Monats-Ansicht auf den Kalendertag (vorher gab es für `'all'` gar kein Bucketing, `bucketStart` gab den rohen Timestamp zurück); die kontinuierliche Zeitachse stapelte alle Buchungen mit identischem Zeitstempel (z. B. ein Saisonabschluss) auf **eine** X-Position („ein Bucket"). `BalanceHistoryChart` nutzt jetzt für **alle** Granularitäten den Bucket-Index für die X-Position (`xS`/`buildPath`/Label-Owner-Logik vereinheitlicht, `isAll`-Zeitproportional-Zweig + `xEnd`/`xSpan` entfernt); `'all'` behält seine eigene scrollbare Breite (eine Spalte je Bucket) und die fixierte Y-Achse. Gleiche `clusterPoints`-Konsequenz: gleicher-Tag-Punkte in „Alle" verschmelzen jetzt zu einem klickbaren „×N"-Marker. (2) **Mitglied-Ansicht hat jetzt drei Linien** statt zwei: „Eingezahlt" (kumulierte Zahlungen), „Strafen" (kumulierte verbuchte Strafen, positiv steigend, rote Linie via `KIND_META.penalty.color`) und „Saldo" (= Eingezahlt − Strafen, hervorgehoben `var(--kce-primary)`) — der Abstand zwischen Eingezahlt- und Strafen-Linie ist der Saldo. Neuer `threeLine`/`overlayLabel`-Prop auf `BalanceHistoryChart`; die Strafen-Linie = `p.actual − p.virtual` (kein neues Event-Stream nötig, rein aus den bestehenden Dual-Series-Feldern abgeleitet), Strafen-Marker sitzen auf der Strafen-Linie, Detail + Legende zeigen alle drei Werte. Kasse-Ansicht bleibt zweizeilig (`threeLine=false`). Neuer i18n-Key `treasury.history.penaltiesMember` („Strafen"/„Penalties"). (3) **Kasse-Schulden-Punkte spielerbezogen:** `GET /club/treasury-debt-timeline` liefert pro Checkpoint jetzt `member_id`/`member_name` (Mitglied, dessen offener Saldo sich bewegt hat — die Änderung seiner Schuld = die Änderung der Club-Gesamtschuld, da pro Event nur ein Mitglied wechselt); `debtEventsFromTimeline` setzt `label = member_name`, sodass die clubweite Schulden-Linie beim Antippen dem Spieler zugeschrieben wird (vorher `label: ''`). Vitest: `bucketStart`/`clusterPoints`-`'all'`-Tests auf Bucketing umgestellt + `debtEventsFromTimeline`-Label-Tests (balanceHistory.test.ts, 38 Tests), 3 neue TreasuryPage-Tests (Schulden-Punkt zeigt Mitgliedsname, Mitglied-Drei-Linien-Legende); pytest: `TestTreasuryDebtTimeline` prüft `member_id`/`member_name`. **Gäste-Deckel auf der Mitglied-Linie:** `GET /club/member-penalties/{mid}` lieferte pro Strafe die ungedeckelte Rohsumme (`_penalty_euro`), sodass die Strafen-/Saldo-Linie eines Gastes im 👤 Mitglied-Modus über den tatsächlichen Gäste-Saldo hinaus lief. Der Endpoint liefert jetzt pro Strafe deren **marginalen Saldo-Beitrag**: für Gäste per-Abend gedeckelt (`min(neu, cap) − min(alt, cap)` mit laufender Roh-Summe je `evening_id`, identisch zur `treasury-debt-timeline`-Logik), Abwesenheitsstrafen (`player_id null`) bei Gästen ausgeschlossen — exakt wie `get_guest_balances`; reguläre Mitglieder bleiben ungedeckelt inkl. Abwesenheitsstrafen (wie `get_member_balances`). Rein Backend (`amount`-Semantik: Roh → Saldo-Beitrag); `memberPenaltyEvents` konsumiert `amount` unverändert, kein Frontend-Logik­wechsel. pytest: 1 neuer `TestMemberPenalties`-Test (Gast-Strafen 3+4 → gedeckelt 3,0/2,0; Abwesenheit ausgeschlossen). |
| 4  | **Mitglieder-Konten & Abrechnung** | ✅      | member_payment Tabelle, Salden-Endpoint, Zahlungen in MembersPage                                     |
| 5  | **Historie**                       | ✅      | Detail-Ansicht, Wiedereröffnen, Löschen, Nachtragen-Sheet; Wiedereröffnen blockiert wenn anderer Abend offen; Abend-Beenden-Dialog (EveningPage + EveningHubPage) fragt den **Endzeitpunkt** ab (`ended_at`) — vorausgefüllt mit dem zuletzt gespeicherten Wert oder jetzt, editierbar zum Nachtragen eines vergessenen Beendens; Wert bleibt beim Wiedereröffnen erhalten und wird beim erneuten Beenden ohne Änderung übernommen; automatisch berechnete Abwesenheitsstrafen nutzen `ended_at` statt des Berechnungszeitpunkts als Zeitstempel |
| 6  | **Eigene Historie / Profil**       | ✅      | Persönliche Jahresstatistiken im Profil (Strafen, Abende, Siege, Bier)                                |
| 7  | **Statistiken & Analyse**          | ✅      | Count-Mode-Strafen (Anzahl × unit_amount) zählen jetzt korrekt zum €-Total im Jahresranking + persönlichen Stats (vorher nur euro-Mode → fehlende Strafen); Abwesenheitsstrafen (player_id=None, regular_member_id) werden dem Mitglied im Jahresranking + persönlichen Stats zugerechnet (vorher nur im Gesamttotal); Jahresranking mit CSS-Balken, Jahresauswahl, alle Mitglieder ein-/ausklappbar; SVG-Donut-Chart (Strafenverteilung pro Spieler), SVG-Balkenchart (Strafen pro Abend im Jahr), HTML-Podium (Top 3 Jahresranking); Abend-Detail: 🏆-Karte öffnet GamesDetailSheet (Spiele mit Status, Sieger, Punkten, Wurf-Summary); Spiele-Kachel zeigt nur die Gesamtzahl, wenn alle Spiele beendet sind, sonst `fertig/gesamt`; klickbare Punkte auf der kumulativen Strafenkurve zeigen die zugehörige Strafe (Zeit, Spieler, Icon, Typ, Betrag); 📋 Verlauf-Sheet (PenaltyTimelineSheet) listet alle Abend-Strafen chronologisch mit klickbarem Spieler-Chip → EveningPlayerDetailSheet; per-Spieler-Strafen im Detail-Sheet ebenfalls chronologisch sortiert |
| 8  | **Push Notifications**             | ✅      | Web Push mit Deep Links; Kategorien-Präferenzen pro User; 4 neue Trigger (König, Abend start, neue Mitglieder, Strafe storniert); VAPID-Backend, ProfileSheet-Toggle+Prefs, SW-Handler; Missed-push-Speicherung in IndexedDB bei geschlossener App; Hybrid-Ladeweg via notification_log (Migration 035): Benachrichtigungen werden server-seitig gespeichert und beim App-Start über API geladen (auch ohne PWA/SW); Deep-Link-Fix (absolute URL in SW navigate()); Glocke immer sichtbar für eingeloggte User; Präzise Deep-Links: Kommentar/Reaktions-Benachrichtigungen navigieren direkt zum Sub-Tab + Item (scroll + Aufhell-Animation auf Kachel + Kommentar-Expand); URLs inkl. `item=ID&comment=ID`; unterstützte Typen: announcements, trips, highlights; `comments`-Präferenz deaktivierbar (Kommentar-Antworten + Reaktionen). **E-Mail-Benachrichtigungen + Zustellweg-Umschalter (off/push/email):** Jede Kategorie-Präferenz ist statt eines Booleans jetzt ein Zustellweg `off`/`push`/`email` (rückwärtskompatibel: alte `true`→`push`, `false`→`off`; `_normalize_prefs` in `push.py` normalisiert beim GET, `PushPreferencesUpdate`-Felder sind `Optional[str]` und werden gegen `{off,push,email}` validiert). Zentrale Zustellung konsolidiert in `core/push.py::notify_user(db, user, …, category)` — resolved den Kanal (`_user_channel`, Default `push`), loggt immer in `notification_log` (außer `off`), und sendet dann **entweder** Web-Push **oder** E-Mail; alle `push_to_*`-Wrapper (`push_to_regular_member`/`push_to_user`/`push_to_club_admins`) rufen `notify_user`, `push_to_club` behält seinen parallelen Push-Pfad und ergänzt E-Mail; `send_upcoming_evening_reminders` (reminders.py) nutzt ebenfalls `notify_user` statt direktem `_send_one`. **E-Mail-Server pro Verein** (kein Migration — in `ClubSettings.extra["email"]`, gleiches Muster wie Erinnerungen): `core/email.py` (`get_club_email_config`, `send_club_email` via stdlib `smtplib` mit STARTTLS/SSL, `build_email_bodies` = Text+HTML mit optionalem Button-Link aus `settings.APP_BASE_URL`). Das SMTP-Passwort wird **verschlüsselt gespeichert** (`core/crypto.py`, `MultiFernet`; `enc:v1:`-Präfix, transparenter Klartext-Fallback für Altbestände → Re-Encrypt beim nächsten Speichern; Entschlüsselung erst in `send_club_email`). **Rotierbarer Schlüssel:** `SECRETS_ENCRYPTION_KEY`-env = kommagetrennte Fernet-Schlüsselliste (erster verschlüsselt, alle entschlüsseln → Rotation: neuen Schlüssel voranstellen, alten behalten bis alle Secrets neu gespeichert sind); leer → Schlüssel aus `SECRET_KEY` abgeleitet. Key-Generator `app/scripts/generate_secret_key.py`. Admin-Endpoints `GET/PATCH /club/email-settings` (Passwort wird nie zurückgegeben, nur `password_set`; PATCH überschreibt Passwort nur bei nicht-leerem Wert und verschlüsselt es vor dem Speichern) + `POST /club/email-settings/test` (Test-Mail an eigene/angegebene Adresse, 400 bei fehlender Konfiguration oder SMTP-Fehler). `GET /push/status` liefert zusätzlich `email_configured`. Frontend: `NotificationChannel`-Typ, `ChannelToggle` (🔕/🔔/✉️-Segment-Control, E-Mail-Segment nur wenn `email_configured`) ersetzt den Boolean-`PushToggle` in ProfileSheet; `EmailSettingsCard` (SMTP-Formular + Test-Knopf) in ClubAdminPage; neue i18n-Keys `push.channel.*`/`email.*`. pytest: `test_email.py` (22 Tests — Config-Resolution, Body-Build, SMTP-Send mocked, Endpoints inkl. Auth-Guards, Kanal-Dispatch), Push-Routes-/Reminder-Tests auf Kanal-Semantik umgestellt. Vitest: ChannelToggle-Sichtbarkeit/Auswahl, EmailSettingsCard (Render/Prefill/Save/Test), API-Client-E-Mail-Methoden. `APP_BASE_URL`-env für absolute Links in E-Mails (optional). **Vereins-Theming & Empfänger-Locale für alle E-Mails:** neuer `core/i18n.py` — winzige DE/EN-String-Tabelle + `format_date`/`format_money` (bewusst systemunabhängig, kein `locale`-Modul, da im Slim-Docker-Image unzuverlässig). `core/email.py::email_theme(club)` löst Grundfarbe (`ClubSettings.primary_color`), Kontrast-Textfarbe (Luminanz-basiert), Vereinsname + absolutes Logo auf; `_header_html` rendert eine Kopfzeile in der Markenfarbe (Logo oder Name), `build_email_bodies` nutzt jetzt Vereinsfarbe + lokalisierten Button/Footer statt hartkodiertem `#e8a020`/Deutsch. `notify_user`/`push_to_club` (push.py) reichen `theme=email_theme(user.club)` + `locale=user.preferred_locale` an `send_notification_email` durch. **Personalisierte E-Mail-Zusammenfassung (Digest):** Migration 051 (`user.last_digest_at`). Pro-Mitglied-Kadenz `digest_frequency` ∈ `off`/`daily`/`weekly`/`monthly` (in `push_preferences`, Default `off` — opt-in). `core/digest.py`: `is_digest_due(user, now)` (Intervall seit `last_digest_at`, 2h-Toleranz, erste Zusammenfassung sofort fällig), `build_digest(db, user, since, now, locale)` sammelt personalisiert die Änderungen seit `since` (Kegelabende neu/aktualisiert/abgeschlossen via `created_at`/`updated_at`/`ended_at`; eigene Strafen inkl. Abwesenheit; eigene `MemberPayment`-Buchungen; Community = neue Kommentare + Item-Reaktionen von Vereinsmitgliedern, deep-linked via `comments._parent_url`) plus Konto-Übersicht (`balance`/`penalty_total`/`paid_total`), gedeckelt auf `MAX_ROWS=15` je Sektion, jeder Eintrag mit Deep-Link (`/#schedule?evening=`, `/#treasury:accounts?member=`, `_parent_url`); gibt `None` zurück wenn nichts Neues (leere Digests werden übersprungen). `core/email.py::build_digest_email(theme, data, locale)` rendert Subject/Text/HTML themed+lokalisiert. `send_all_digests(db, now)` iteriert fällige aktive Mitglieder mit konfiguriertem Club-SMTP, sendet und setzt `last_digest_at`; Scheduler-Job täglich 08:00 (`run_daily_digests`). API: `GET/PATCH /push/preferences` inkl. `digest_frequency` (validiert gegen `{off,daily,weekly,monthly}`), `POST /push/digest/test` (Member, force — sendet auch bei leerem Inhalt die Konto-Übersicht, ohne die Kadenz zurückzusetzen; 400 ohne Config/Member/E-Mail). Frontend: `DigestFrequency`-Typ, Digest-Karte in ProfileSheet-Einstellungen (Häufigkeits-Pills + „jetzt senden"-Test, nur bei `email_configured`), `api.sendTestDigest`, i18n `digest.*`. pytest: `test_digest.py` (16 Tests — i18n-Helper, Due-Logik, Builder inkl. Empty-Skip & Alt-Aktivitäts-Ausschluss, Theme, Rendering, `send_all_digests` Kadenz-Advance, Preferences-Roundtrip, Test-Endpoint-Guards+Send). Vitest: 4 ProfileSheet-Digest-Tests + API-Client-Test. Push UND E-Mail gleichzeitig (Multi-Channel):** Der Zustellweg pro Kategorie ist nicht mehr ein einzelner, sich gegenseitig ausschließender String (`off`/`push`/`email`), sondern eine **Liste aktivierter Kanäle** (`[]` = aus, `["push"]`, `["email"]`, `["push","email"]` = beide) — ein Mitglied kann eine Benachrichtigung jetzt gleichzeitig als Push **und** E-Mail bekommen. Zentrale Normalisierung `resolve_channels(value, default=("push",))` in `core/push.py` ist voll rückwärtskompatibel (bool `True`→`["push"]`/`False`→`[]`, String `"off"`→`[]`/`"push"`→`["push"]`/`"email"`→`["email"]`, Liste → gefiltert/dedupliziert auf gültige Kanäle in kanonischer Reihenfolge push, email). `notify_user`/`push_to_club` resolven jetzt eine Kanal-Liste (`_user_channels`) und senden an **jeden** aktiven Kanal (loggen aber nur einmal in `notification_log` für die In-App-Glocke, nicht pro Kanal). `_user_wants` = „irgendein Kanal an". API: `GET/PATCH /push/preferences` geben/nehmen pro Kategorie eine Kanal-Liste; `_normalize_prefs`/`_DEFAULT_PREFS` (Default `["push"]`) angepasst; `PushPreferencesUpdate`-Felder sind `Union[list[str], str, bool]` und werden serverseitig via `resolve_channels` saniert (unbekannte Kanäle verworfen, leere Liste = aus) — ältere Clients, die noch einen String/Bool senden, werden bei einem Rolling-Deploy toleriert statt mit 422 abgelehnt. Frontend: `NotificationChannel` = `'push' | 'email'` (kein `'off'` mehr — Aus = leere Liste), neuer `ChannelPref = NotificationChannel[]`; die segmentierte `ChannelToggle` (Einfachauswahl 🔕/🔔/✉️) ist durch `ChannelToggles` ersetzt — zwei unabhängige Umschalter (🔔 Push, ✉️ E-Mail, E-Mail nur wenn `email_configured`), jeder toggelt seinen Kanal in/aus der Liste; `value` wird defensiv auf ein Array normalisiert (`Array.isArray(value) ? value : []`), sodass Alt-/Teilwerte nicht crashen. `setPushChannels(key, channels)` sendet die Liste per PATCH. i18n-Hinweis `push.channelHint` umformuliert („Push und/oder E-Mail … Beides aus = keine Benachrichtigung"). **Test-E-Mail-Ziel** (unverändert, zur Klarstellung dokumentiert): `POST /club/email-settings/test` sendet an `data.to`, falls angegeben, sonst an die eigene Account-E-Mail des auslösenden Admins (`user.email`) — nicht an alle Mitglieder. pytest: 73 (test_email.py + test_push_routes.py) inkl. neuem `test_notify_user_sends_both_when_push_and_email` (beide Kanäle feuern, nur ein Log-Eintrag) + Kanal-Listen-Round-Trip/Legacy-String-Toleranz/Invalid-Drop-Tests. Vitest: ProfileSheet-Kanal-Tests auf unabhängige Toggles umgestellt (E-Mail wird zu Push hinzugefügt statt ersetzt, letzten Kanal abschalten → leere Liste), apiMethods-Test auf Kanal-Liste. **Digest interaktiver: Absender-Avatar, Öffnen-Button, gebündelte Neuigkeiten-Threads:** `_header_html` (core/email.py) zeigt Vereinslogo/-Initiale jetzt als runden Avatar neben dem Vereinsnamen statt Logo-oder-Name als Alternative — größtmögliche Annäherung an einen „Absender-Avatar" innerhalb des Mail-Bodys, da Mail-Clients einen echten Inbox-Avatar nur über Gravatar/BIMI zulassen (außerhalb der App-Kontrolle); Änderung wirkt automatisch auf alle Mails (Digest + einzelne Benachrichtigungen), da `build_email_bodies` denselben Header nutzt. `build_digest_email` hat jetzt einen „Öffnen"-Button (`digest.button.open`, bisher ungenutzter i18n-Key) direkt unter der Begrüßung, der zur App-Startseite verlinkt (nur wenn `APP_BASE_URL` gesetzt ist). **Community-Sektion als Neuigkeiten-Threads:** `_community_news` (core/digest.py) gruppiert neue Kommentare/Reaktionen jetzt pro Ursprungs-Objekt (Ankündigung/Kegelfahrt/Highlight) statt eine Zeile pro Einzel-Event zu zeigen — eine Zeile pro Thread mit Titel (`_parent_title`), Typ-Icon (📣/🚌/✨) und Aktivitäts-Summary (`💬 N · ❤️ M`), deep-linked zur neuesten Aktivität im Thread (`_parent_url` mit Kommentar-ID, falls die neueste Aktivität ein Kommentar ist, sonst ohne Kommentar-Bezug). Titel-lose Highlights (nur Medien, kein Text) fallen auf `digest.community.untitled` zurück; die bisherigen granularen `digest.community.comment`/`.reaction`-i18n-Keys sind entfallen. pytest: 7 neue Tests (`test_digest.py` — Thread-Gruppierung mit kombiniertem Kommentar-/Reaktions-Count, Deep-Link zur neuesten Aktivität, Reaktion-als-neueste-Aktivität-Fallback ohne Kommentar-Link, Titel-los-Fallback, Öffnen-Button mit/ohne `APP_BASE_URL`, Avatar-Header mit Logo/mit Initialen-Fallback). **Eigene Domain pro Verein für E-Mail-Links:** `email.base_url` (neues Feld im bestehenden `ClubSettings.extra["email"]`-Blob, `EMAIL_CONFIG_FIELDS`/`_EMAIL_DEFAULTS`/`EmailSettingsUpdate` erweitert) überschreibt die serverweite `APP_BASE_URL` für alle Links (inkl. Logo) in den E-Mails eines Vereins — z. B. wenn der Verein per CNAME eine eigene Domain auf die Instanz zeigen lässt. `email_theme(club)` löst `base_url` auf (Vereinswert vor `APP_BASE_URL`-Fallback) und reicht ihn als `theme["base_url"]` durch; `_abs_link(url, base_url=None)` nimmt den Override als zweites Argument entgegen, alle Aufrufer (`build_email_bodies`, `_digest_row`, digest-CTA) reichen `theme["base_url"]` durch. Neues Feld „Eigene Domain (optional)" in `EmailSettingsCard` (ClubAdminPage) mit Hinweistext, leer = Standard-URL. **Community-Zeilen zeigen jetzt eine Textvorschau:** `_community_news` (core/digest.py) trackt zusätzlich `latest_snippet` pro Thread (Text des neuesten Kommentars, auf 80 Zeichen gekürzt via neuem `_snippet()`-Helper; `None` bei reiner Reaktion als neueste Aktivität oder medien-only Kommentaren) — Ziel: Threads lesen können, ohne die App zu öffnen. `_digest_row` (core/email.py) bekommt einen optionalen `snippet`-Parameter und rendert ihn als eigene, kursive Zeile unterhalb des Titels (Text-Version: eingerückte Zeile in Anführungszeichen). **PWA Link Capturing:** `vite.config.ts`-Manifest bekommt `scope: '/'` + `capture_links: 'existing-client-navigate'` (Declarative-Link-Capturing-Spec, noch nicht in `vite-plugin-pwa`s TS-Typ — `@ts-expect-error` kommentiert) — installierte PWA öffnet einen angeklickten Deep-Link (z. B. aus einer Digest-Mail) auf unterstützenden Browsern (Chrome/Edge Desktop+Android) im bestehenden App-Fenster statt einem neuen Browser-Tab; ohne Wirkung auf iOS/Safari (WebKit unterstützt das Feature nicht, kein Client-seitiger Workaround möglich). pytest: 9 neue Tests (`TestPerClubBaseUrl` in `test_email.py` — Theme-Override/Fallback, Logo-URL, `_abs_link`, Digest-Links nutzen Vereins-Domain; `test_base_url_round_trips`/`_defaults_empty` in `TestEmailSettingsEndpoints`; `test_digest.py` — Snippet im Thread, Kürzung auf 80 Zeichen, kein Snippet bei Reaktion, Snippet-Rendering in `build_digest_email`). Vitest: 2 neue `ClubAdminPage`-Tests (Vorbefüllung + Speichern der eigenen Domain). |
| 9  | **Offline-Sync**                   | ✅      | Temp-ID-System: Abend offline starten (pendingStore + negative tempId), Gast-Member offline anlegen; flush rewrite tempId→realId in Pfaden und Bodies; IndexedDB-Queue für alle Abend-Mutations (Strafen, Getränke, Spiele, Spieler, Highlights, Teams, RSVP, …); navigator.onLine-Sofortcheck verhindert Hängen; OfflineQueuedError; Auto-Flush on reconnect; kegelkasse:temp-id-resolved Event; isPending-Badge im Abend; OfflineNotice + disabled Buttons für Admin/Finance-Bereiche |
| 10 | **Logo-Upload**                    | ✅      | Admin-Upload (JPEG/PNG/WebP/GIF/SVG, max 5 MB); POST /club/logo + DELETE /club/logo; Docker Volume club_uploads:/app/uploads; Serve via /uploads StaticFiles; Logo in App-Header statt animierter Kugel wenn gesetzt; Vorschau + Entfernen-Button in ClubSettingsTab |
| 11 | **Emoji Picker**                   | ✅      | `emoji-picker-react` v4, EmojiPickerButton-Komponente, Icon- & Insert-Modus, 5 Verwendungen         |
| 13 | **Präsident**                      | ✅      | Jährliches Präsidentenspiel (🎯-Flag), club_president-Tabelle, Historie-Badge, Präsidenten-Tab       |
| 14 | **Filter**                         | ✅      | Suchfelder in allen Listen-Ansichten: MembersPage (Name/Spitzname), HistoryPage (Datum/Lokal), TreasuryPage (Konten + Buchungen), StatsPage (Jahresranking nach Name), CommitteePage (Ankündigungen + Kegelfahrten nach Titel/Text) |
| 30 | **Vergnügungsausschuss**           | ✅      | VA-Mitglieder (is_committee Flag), Kegelfahrten (club_trip), Ankündigungen (club_announcement) mit Push; Migrationen 032–034; 🚌-Tab für alle; VA-Verwaltung im Verein-Tab |
| 16 | **Cleanup / Fehlerhandling**       | ✅      | Konsistente Fehlerbehandlung: alle catch-Blöcke verwenden toastError(); MembersPage + ProfileSheet korrigiert; toastError unterscheidet UnauthorizedError, OfflineQueuedError und generische Fehler |
| 17 | **Logging**                        | ✅      | Python stdlib logging mit LOG_LEVEL env-var (config.py); logging.basicConfig in main.py; strukturierte Logger in auth, evenings, scheduler, club, committee, superadmin, push, schedule, reports, backups; 5xx-Middleware-Logging |
| 32 | **Datenbank-Backups**              | ✅      | pgbackrest in custom db-Image (postgres:16 + pgbackrest + Python mgmt-server auf :8089); WAL-Archivierung → PITR; APScheduler-Cron-Job Full-Backup; Superadmin-Tab: Backup-Liste (Label, Typ, Größe, PITR-Fenster), Manuell auslösen; Retention per PGBACKREST_REPO1_RETENTION_FULL; S3 via PGBACKREST_REPO1_* env-vars |
| 18 | **Testing**                        | 🚧      | Vitest: cameraEngine (readFrame, digit/pin/lamp detection), turnOrder (alternating/block), API client (push, club, logo), alle Page-Komponenten (EveningPage, TreasuryPage, SchedulePage, HistoryPage, MembersPage, GamesPage, CommitteePage, ClubAdminPage, LoginPage, StatsPage, HistoryPage, ProfileSheet, TabletQuickEntryPage, CameraCapturePage). pytest: alle Backend-Endpunkte vollständig abgedeckt (379 Tests); push (preferences, debug, recent, mark-read, trigger-reminders), comments (edit, item-reactions), sync (add/delete penalty+drink), reports (xlsx+pdf), backups (mocked pgbackrest). Uncovered — see Testing-TODO below. |
| 33 | **Kamera-Wurf-Erkennung (Echtzeit)** | ✅    | Alle 4 Phasen implementiert. Architektur: Kamera-Gerät (Stativ, Kiosk-Modus) + Tablet (Schnellerfassung + Wurf-Management). CameraCapturePage: Kiosk-Modus (auto-submit, kein Bestätigungs-Overlay, Video fullscreen, Exit-Button). TabletQuickEntryPage: kombiniert Strafen/Getränke + Kamera-Wurf-Strip (Live-Würfe via SSE, Spieler-Zuordnung, Widerruf-Button) + Spieler-Reihenfolge (Abwechselnd / Block-Modus, aktuelle Spielerin hervorgehoben, nächste angezeigt, manuelle Weiter-Taste) + Spiel-Beenden (Gewinner-Wahl inline). game_throw_log.player_id (Migration 038, FK evening_player, SET NULL); DELETE /evening/{eid}/games/{gid}/throws/{tid}; Phase 4: game.active_player_id (Migration 041, FK evening_player SET NULL); PATCH /evening/{eid}/games/{gid}/active-player; Tablet schreibt aktiven Spieler bei Zug-Wechsel; Kiosk liest active_player_id + wählt Spiel automatisch. |
| 19 | **Bezahllink**                     | ✅      | PayPal.me-Link im Profil, Zahlung melden (PaymentRequest), Admin bestätigt/lehnt ab in Kasse        |
| 20 | **Abwesenheiten verwalten**        | ✅      | Spieltermine & RSVP (SchedulePage); Abwesenheitsstrafen auto beim Start-aus-Termin; no_cancel_fee nur wenn RSVP vorhanden; Gäste ohne regular_member_id werden beim Start automatisch als RegularMember angelegt |
| 21 | **Schulden-Erinnerungen**          | ✅      | APScheduler (tägl. 09:00); 5 Typen: Schulden wöchentlich, Kegeln in X Tagen (per-user), RSVP, Schulden am Kegeltag, Zahlungsanfragen-Nudge; Toggle-Fix; Broadcast-Push; Admin-Konfiguration in ClubAdminPage |
| 22 | **Import / Export**                | ✅      | Excel (.xlsx) + PDF-Export: Mitglieder-Konten, Buchungen, Strafen nach Person/Abend, Abend-Übersicht; Jahresfilter; auto. Bericht-Push vor Kegelabend (config in ClubAdmin); openpyxl + fpdf2. **Jahresauswahl im Export-Sheet dynamisch:** vorher hartkodiert auf die letzten 5 Kalenderjahre ab `currentYear` — zeigte Jahre ohne jede Buchung und ließ ältere Jahre mit Buchungen außen vor. `exportYears` in `TreasuryPage.tsx` leitet die distinkten Jahre stattdessen aus `date ?? created_at` aller `allPayments` + `expenses`-Einträge ab (gleiches Fallback-Muster wie `mergedBookings`), absteigend sortiert; die `allPayments`-Query lädt jetzt zusätzlich, wenn `showExportSheet` offen ist (vorher nur auf den Tabs Übersicht/Buchungen), da der Export-Button unabhängig vom aktiven Tab im Kopfbereich sichtbar ist. |
| 23 | **Pins**                           | ✅      | Vereinsnadeln: Träger zuweisen, Abend-Alert bei anwesendem Träger, Strafe per Knopfdruck eintrabar; PinsAlert zeigt ✓ wenn Strafe bereits eingetragen; Pin-Icons neben Kegelname; Präsident-Badge (🎯) ebenfalls inline |
| 24 | **iCal Export**                    | ✅      | Öffentlicher Abo-Link (webcal://) mit Secret-Token; Uhrzeit pro Termin; Club-Standard 20:00; 3 Migrationen (022–024); Soft-Delete von Terminen (STATUS:CANCELLED im Feed, Migration 029) |
| 25 | **Bug-Fixes Batch 1**              | ✅      | Statistiken-Label, leere Spiele-State, Teamzuordnung, Vergangenheitsdatum, Abend-Doppelstart, Quick-Start, Strafendatum (Admin), Ausgaben-Datum (Migration 028) |
| 26 | **Bug-Fixes Batch 2**              | ✅      | TreasuryPage Buchungen-Datum konsistent rechts (unter Betrag) für alle Eintragstypen; NotificationPanel markAllRead in useEffect (React-Renderbug); leere-Benachrichtigungen-Hinweis; Notification-Sync-Fix: beim Abend-Close wird ['evenings']-Liste sofort invalidiert + Offline-Queue geflusht, damit SchedulePage keine veraltete "aktiver Abend"-Karte zeigt und der OfflineBanner keine veralteten ausstehenden Änderungen anzeigt; Abend-Detail (HistoryPage + SchedulePage): beendete Spiele werden nach `started_at` (Fallback `sort_order`) sortiert statt in Backend-Reihenfolge angezeigt |
| 27 | **Highlights**                     | ✅      | Abend-Highlights erfassen (✨): evening_highlight-Tabelle, CRUD-Endpoints, Freitext-Input am Abend; Migration 030 |
| 28 | **Abend nur via Termin starten**   | ✅      | create_evening auf require_club_admin gesetzt; ad-hoc-Formular bleibt für Admins erhalten             |
| 29 | **Tablet Schnellerfassung**        | ✅      | Vollbild-Overlay (⚡) für Landscape-Modus; 3-Spalten-Layout: Spieler (Auswahl) \| Strafen + Getränke \| **Übersicht pro Spieler** (tatsächliche Strafen-€-Summe, Spiel-Score live aus letztem Wurf, Getränke-Counts, Plausibilitäts-Check + Gesamtsumme im Footer); iOS safe-area-insets (Notch, Home-Indicator, gerundete Ecken); kein Scrollen erforderlich; letzte Einträge unten; 0€-Strafen ausgeblendet; Strafen-Buttons vergrößert. **Getränkerunden direkt im Strafen-Panel** als 🍺/🥃-Buttons — gleiche Spieler-Mehrfachauswahl wie Strafen, kein Bottom-Sheet und kein versteckter Header-CTA mehr (Runde wird sofort für die ausgewählten Spieler gebucht, Auswahl danach zurückgesetzt). Übersicht-Spalte nach Strafen-€ absteigend sortiert mit FLIP-Reorder-Animation. **Footer-Σ/Ø korrigiert:** Die vorläufige Übersicht wendet **keinen Gäste-Deckel** an (der Deckel ist eine reine Kassen-/Abrechnungsregel) — ein Gast mit 70 € erscheint mit 70 € in seiner Zeile, im Σ-Total und im Ø-Durchschnitt. Σ Strafen = Summe der tatsächlichen Strafen der **anwesenden** Spieler (Abwesenheits-/Nachtrags-Strafen mit `player_id=null` bleiben außen vor); Ø Strafen = Σ ÷ anwesende Spieler. **Team-Guard beim Spielstart:** „▶ Start" (offenes Team-Spiel) sowie das Auto-Starten beim Anlegen eines Team-Spiels aus einer Vorlage prüften bisher `teams.length` nicht — ein Team-Spiel ohne angelegte Teams ließ sich trotzdem starten (bzw. wurde beim Anlegen sofort automatisch gestartet), obwohl die Team-Zuordnung fehlte; die bereits vorhandene ⚠️-Warnbanner-Anzeige (`game.teamsRequired`) blockierte den Start selbst nicht. `handleStartGame` und `handleCreateGame` prüfen jetzt `winner_type === 'team' && teams.length === 0` und zeigen stattdessen einen Toast (`game.teamsRequired`) statt `api.startGame` aufzurufen; das neu angelegte Spiel bleibt dabei im Status `open` (nicht automatisch gestartet). Warnbanner-Bedingung ebenfalls von `turn_mode === 'block'` auf `winner_type === 'team'` korrigiert, damit sie auch für `alternating`-Team-Spiele greift. Vitest: 3 neue Tests. **Derselbe Guard fehlte auch im regulären Spiele-Tab (`GamesPage.tsx`):** `startGame()`s bestehender `unassignedPlayers`-Check (`teams.length > 0 ? players.filter(...) : []`) war bei komplett fehlenden Teams immer eine leere Liste — der Guard griff nur, wenn Teams zwar existierten aber nicht alle Spieler zugewiesen waren, nicht im (häufigeren) Fall gänzlich fehlender Teams; das war der eigentlich gemeldete Bug (Spielstart ohne Teams weiterhin möglich trotz Fix im Tablet-Schnellerfassungs-Flow). `startGame()` prüft jetzt zuerst `winner_type === 'team' && teams.length === 0` (Toast `game.teamsRequired`, kein API-Call) vor dem bestehenden Zuweisungs-Check; offene Team-Spiele ohne Teams zeigen zusätzlich denselben ⚠️-Warnhinweis direkt auf der Spiel-Karte. Vitest: 3 neue Tests (Warnhinweis, blockierter Start ohne Teams, blockierter Start bei nicht zugewiesenen Spielern). **Guard gilt für jedes Spiel, nicht nur `winner_type === 'team'`:** Teams werden einmal für den ganzen Abend angelegt (erster Konfigurationsschritt in `EveningPage.tsx`, noch vor der Spielerzuordnung), nicht pro Spiel — die `winner_type === 'team'`-Einschränkung in beiden Guards (`teams.length === 0` und `unassignedPlayers.length > 0`) war deshalb sachlich falsch und wurde entfernt; jedes offene Spiel (auch individuelle) lässt sich jetzt nur starten, wenn der Abend Teams hat **und** alle Spieler einem Team zugewiesen sind. `TabletQuickEntryPage.tsx` bekommt dafür dieselbe `unassignedPlayers`-Berechnung wie `GamesPage.tsx` sowie einen zweiten Warnbanner (analog zum bestehenden Seiten-weiten Hinweis in `GamesPage.tsx`) und denselben `team.cannotStartUnassigned`-Toast bei Start bzw. Auto-Start. Vitest: 6 weitere Tests (individuelle Spiele ohne Teams/mit nicht zugewiesenen Spielern in beiden Dateien). |
| 31 | **Navbar-Farben & Farbpalette**    | ✅      | Navbar nutzt CSS-Variablen (--kce-surface2, --kce-border, --kce-primary); Live-Vorschau bei Farbänderung; Paletten-Generator in Erscheinungsbild: Grundfarbe wählen + Vorschläge (Warm/Kontrast/Triade/Weich) oder Zufallspalette |
| 34 | **Kommentare & Reaktionen**        | ✅      | Instagram-Style Kommentar-Threads auf Highlights, Ankündigungen und Kegelfahrten; Autoren-Avatar (Bild oder Initialen), Chat-Bubble-Layout, relative Zeitangaben, ❤️ als primäre Reaktion rechts am Kommentar, weitere Emojis als Pill-Badges in der Aktionszeile; Pill-Input mit User-Avatar links; Bild vor Caption; 💬 (N) immer sichtbar inkl. (0); Antworten-Link inline; 🗑️ Löschen mit Inline-Bestätigung; bearbeiten (edited_at); max Tiefe 1 im UI (tiefere Nesting-Ebenen werden geflattened); Deeplink-Highlight beleuchtet Kommentar und Ursprungskachel; Migration 042+044; pytest: 28+ Tests; `comments`-Präferenz zum Deaktivieren von Kommentar/Reaktions-Benachrichtigungen |
| 35 | **Medien-Upload**                  | ✅      | Self-hosted Bild-Upload (JPEG/PNG/WebP/GIF, max 10 MB); POST /uploads/media; Bilder in Highlights, Ankündigungen und Kommentaren; MediaUploadButton-Komponente mit Vorschau; Migration 043 (media_url an highlight/announcement/comment + text nullable); pytest: 8 Tests |
| 36 | **Item-Reaktionen**                | ✅      | ❤️ als primäre Reaction-Schaltfläche auf Highlights, Ankündigungen und Kegelfahrten; weitere Emoji-Reaktionen als sekundäre Pill-Badges; ItemReactionBar-Komponente; VALID_PARENT_TYPES: highlight/announcement/trip; Migration 044 (item_reaction-Tabelle) |
| 37 | **Superadmin Club-Verwaltung**     | ✅      | Verein umbenennen (Name + Slug) via PATCH /superadmin/clubs/{id}; Verein löschen (Kaskaden-Delete aller Daten) via DELETE /superadmin/clubs/{id}; UI in SuperadminClubsTab (✏️-Button + ×-Button mit Bestätigungs-Sheet); pytest: 19 Tests |
| 38 | **Spieler-Performance-Tracking**   | ✅      | Wurf-für-Wurf-Statistiken pro Mitglied über Zeit: Durchschnittswürfe, beste/schlechteste Abende, Trendkurve. Nutzt vorhandene game_throw_log-Daten (Feature 33). Endpoints GET /stats/me/throws + GET /stats/members/{id}/throws; persönliche Statistik-Karte (SVG-Sparkline) im Profil; aufklappbare Wurf-Performance pro Spieler im Jahresranking der StatsPage (inkl. Trend-Chart + Abend-Liste); 9 pytest-Tests. |
| 39 | **Saisonabschluss-Workflow**       | ✅      | Geführter Jahresabschluss für Admins: Neuer Admin-Tab "Saisonabschluss" mit 4-Schritt-Wizard (Bilanz prüfen → Bestätigen → Erledigt). POST /season/close: Bilanzausgleich (MemberPayment-Einträge auf Null), Jahresranking einfrieren (season_snapshot-Tabelle mit JSON-Ranking), Abende archivieren (bulk is_closed), PDF-Jahresbericht per Download-Button. GET /season/snapshots + /season/snapshots/{year}. Migration 046; 14 pytest-Tests; 35 Vitest-Tests (SeasonTab + API-Client). |
| 40 | **Gäste-Management**               | ⬜      | Gastspieler per Link einladen (zeitlich begrenztes Token); Gast sieht eigene Abend-Übersicht (Würfe, Strafen, Getränke) ohne vollen Account; Admin kann Gast nachträglich in reguläres Mitglied konvertieren. Neue Tabelle guest_token; GET /guest/{token}/summary Endpoint. |
| 41 | **Abstimmungen / Umfragen**        | ✅      | Einfach- und Mehrfach-Antwort-Polls für Mitglieder (z. B. Terminwahl, Vereinsentscheidungen). Admin erstellt Poll mit Frage + Antwortoptionen + Modus (single/multi). Mitglieder stimmen ab, Ergebnisse live sichtbar. Neue Tabellen club_poll + poll_option + poll_vote; CRUD-Endpoints unter /committee/polls; Poll-Kacheln im VA-Tab (🗳️) mit Ablaufdatum und Ergebnisanzeige. |
| 42 | **Gast-Kosten-Übertragung**        | ✅      | Admin kann offene Gastkosten in der Kasse auf ein reguläres Mitglied umbuchen (↪️ Übertragen am Gast-Kärtchen in der Übersicht). Doppelte Buchung: Gast bekommt Gutschrift (+amount), Mitglied wird belastet (−amount); beide MemberPayment-Zeilen referenzieren sich in der Notiz. Statistiken/PenaltyLog bleiben unverändert. POST /club/guest-cost-transfer; pytest: 9 Tests; Vitest: API-Client-Test. |
| 44 | **Mitglied aus Verein entfernen**  | ✅      | `DELETE /regular-members/{mid}` konvertiert reguläres Mitglied zu Gast (`is_guest=True`, Login gesperrt, Pins gelöscht) statt vollständiger Deaktivierung — Mitglied bleibt im Gäste-Roster sichtbar und kann weiter an Abenden teilnehmen; **Gäste sind nicht löschbar** (400 „Gäste können nicht entfernt werden — sie bleiben Teil der Vereinshistorie", Knopf im Gäste-Roster entfernt), da ihre Abend-Teilnahmen/Statistiken dauerhaft Teil der Vereinshistorie bleiben; `PATCH /regular-members/{mid}/reactivate` setzt `is_guest=False` + reaktiviert Account; ⬆️-„Zu Mitglied machen"-Knopf im Gäste-Roster (Admin) — Wortwahl/Icon bewusst neutral gegenüber „Reaktivieren", da auch Gäste promoviert werden, die nie Mitglied waren; **Entfernen-Knopf im Mitglieder-Roster bewusst subtil** (⬇️, `btn-secondary` statt rotem ✕/`btn-danger`) und Sheet-Bestätigung `btn-primary`, da die Aktion eine Rückstufung zum Gast ist (reversibel, keine Datenlöschung) statt einer destruktiven Entfernung; Bestätigungs-Sheet bei Entfernen; **Eintrittsbeitrag bei Beförderung**: Bestätigungs-Sheet schlägt einen anteiligen Eintrittsbeitrag vor = Kassenstand (Summe aller Mitgliedssalden inkl. offener Schulden) ÷ Anzahl bestehender Mitglieder; Admin kann den Betrag anpassen oder löschen; bei Bestätigung wird er als Schuld (negativer `MemberPayment`-Eintrag, Notiz „Eintrittsbeitrag") auf dem neuen Mitgliedskonto gebucht — rein frontend-seitig orchestriert über die bestehenden `reactivateRegularMember`- und `createMemberPayment`-Endpoints, kein neuer Backend-Code; pytest: 10 Tests (Entfernen/Reaktivieren/Gast-Schutz); Vitest: 3 neue Tests (Sheet-Vorbefüllung, Bestätigung mit/ohne Beitrag). |
| 45 | **Austritts-Auszahlung**           | ✅      | Beim Entfernen eines Mitglieds: optionale Auszahlung im Bestätigungs-Sheet (vorausgefüllt mit `max(0, payments_total − penalty_total)`); `POST /club/treasury-payout` erzeugt negative MemberPayment-Einträge; pytest: 6 Tests. |
| 46 | **PWA-Installation**               | ✅      | Auto-Installations-Banner (`InstallPrompt`) oben im App-Shell (nach `OfflineBanner`): Chromium → nativer `beforeinstallprompt`; iOS Safari → „Zum Home-Bildschirm"-Anleitung im `<Sheet>` (Teilen → Hinzufügen). Geteilter `usePwaInstall`-Hook (canInstall/isIos/isStandalone/promptInstall) ersetzt die Inline-Detection in ProfileSheet; „App installieren"-Zeile im Profil (✓ Installiert wenn standalone). Banner mit ✕ ausblendbar (localStorage `kce_install_prompt_dismissed`), Profil bleibt Fallback. Vitest: 6 Tests (usePwaInstall) + i18n-Parität. |
| 47 | **Bug-Fixes Batch 3**              | ✅      | `--kce-amber` CSS-Var war nie in `:root` deklariert (nur als Tailwind-`@theme`-Alias `--color-kce-amber`) — alle bestehenden `var(--kce-amber)`-Referenzen in StatsPage/TabletQuickEntryPage/CameraCapturePage/ProtocolPage/GamesPage/CommitteePage/ClubAdminPage/ProfileSheet lösten sich zuvor in nichts auf; jetzt als Alias auf `--kce-primary` deklariert. StatsPage-Gold-Podium/„Ich"-Label nutzten hartkodiertes `#e8a020` statt `var(--kce-primary)` (Gradient-Start-Ton `#c4701a` bewusst hartkodiert belassen, da kein Duplikat eines Tokens). Hartkodierte deutsche Strings statt `t()`: Backup-Leerzustand + Backup-Fehler-Badge in ClubAdminPage (bestehende, bisher ungenutzte Keys `backup.empty`/`backup.error` reaktiviert), Broadcast-Push-Fehlermeldung (neuer Key `broadcast.error`), StatsPage-Ladezustand (`action.loading`). |
| 48 | **Navigation & Auffindbarkeit**     | ✅      | Neuer `'members'`-Nav-Tab (App.tsx) für reguläre Mitglieder — zeigt schreibgeschützt (intern durch `isAdmin(user)` in `MembersPage` gesteuert) das Vereins-Roster; exklusiv zum admin-only `'club'`-Tab, sodass die Tab-Anzahl für beide Rollen bei 6 bleibt. `EveningHubPage`-Tableiste hat jetzt einen „⚙️ Verwalten"-Button (ruft `onNavigate`) — Team-/Spielerverwaltung (`EveningPage`/`'config'`-Route) war vorher nur über die kleine „AKTIV"-Kopfzeilen-Pille erreichbar. Doppelte Abend-beenden/wiedereröffnen-Logik konsolidiert in neuem `frontend/src/hooks/useCloseReopenEvening.ts`, gemeinsam genutzt von `EveningHubPage` und `EveningPage`; `EveningPage`s Inline-Zweistufen-Bestätigung wurde auf dasselbe Sheet-Muster wie `EveningHubPage` umgestellt, kanonisches Schließen-Verhalten löscht jetzt immer `activeEveningId` und invalidiert `['evenings']` + `['schedule']` (vorher divergierten beide Implementierungen). `ClubAdminPage`-Unter-Tab „🚌 VGA" war hartkodiert (kein i18n) — jetzt `t('club.tab.committee')` → „🛠️ VA-Verwaltung", eigenes Icon getrennt vom 🚌 der Kegelfahrten. `StatsPage` in „Abend"/„Jahr"-Tabs aufgeteilt (`useHashTab`, gleiches Tab-Muster wie CommitteePage/ClubAdminPage/TreasuryPage) statt eines langen ungegliederten Scrolls. **Verwalten-Navigation vereinheitlicht (Bug-Fix):** Der separate „⚙️ Verwalten"-Button plus das eigenständige `'config'`-Top-Level-Page-Routing sind entfernt — der doppelte `flex-shrink-0`-Button (Verwalten + Beenden/Wiedereröffnen) neben den `flex-1`-Tabs überschritt auf schmalen Screens die Strip-Breite ohne `overflow-x-auto` und ohne `min-width:0` auf den Tab-Buttons, sodass die Tabs abgeschnitten/nicht scrollbar wirkten; zusätzlich sprang ein Klick auf „Verwalten" auf eine komplett andere Top-Level-Seite (`EveningPage` via `setPage('config')`), wodurch die Hub-Tableiste (Protokoll/Spiele/Highlights) optisch verschwand. „Verwalten" ist jetzt ein vierter gleichwertiger Sub-Tab (`SubTab = 'penalties' | 'games' | 'highlights' | 'manage'`) in `EveningHubPage`, der `<EveningPage/>` „always mounted, toggled via display" einbettet (gleiches Muster wie Protokoll/Spiele) — die Tableiste bleibt dabei immer sichtbar. Tab-Strip hat jetzt `overflow-x-auto` + `min-w-0`/`truncate` auf den Tab-Buttons als Overflow-Fallback. Externe Einstiege (AKTIV-Kopfzeilen-Pille in `App.tsx`, „→ Verwalten"-Hinweis bei nicht zugeordneten Spielern in `GamesPage.tsx`) setzen jetzt `window.location.hash = 'evening:manage'` statt `setPage('config')`; `PageId`/`NAV_PAGES` in `App.tsx` sowie der `'config'`-Page-Switcher-Eintrag sind entfallen. Ohne aktiven Abend rendert `EveningHubPage` direkt `<EveningPage/>` (dessen vollständiges Start-Formular) statt einer eigenen reduzierten Platzhalter-Karte. |
| 49 | **Barrierefreiheit & Responsivität** | ✅    | `--kce-muted` von `#7a6258` (~3.2:1 auf `--kce-bg`) auf `#a08a7e` (~5.6:1, WCAG-AA) angehoben; Chart-Achsenbeschriftungen in StatsPage/TreasuryPage von `fontSize` 8/9 auf 9/10 vergrößert. `components/ui/Sheet.tsx` bekommt echtes Fokus-Management: `role="dialog"`/`aria-modal`/`aria-label`, Fokus wandert beim Öffnen auf das Panel und kehrt beim Schließen zum auslösenden Element zurück (Close-Button von 28px auf 44×44px, `aria-label`); `ProfileSheet.tsx` (eigene Bottom-Sheet-Implementierung, nutzt die geteilte Komponente nicht) bekommt dasselbe Fokus-Management direkt. Donut-Segmente und Punkte in `StatsPage.tsx` sowie Chart-Ereignispunkte in `TreasuryPage.tsx` sind jetzt per Tastatur bedienbar (`tabIndex`, `role="button"`, `aria-label` mit Betrag/Datum, `onKeyDown` für Enter/Leertaste). Wurf-Bearbeiten/Verwerfen-Buttons in `TabletQuickEntryPage.tsx` sowie der Kamera-Schließen-Button in `CameraCapturePage.tsx` haben größere Tap-Flächen (32×32 bzw. 44×44px, vorher ~16-20px) und `aria-label` (bewusst nicht auf volle 44px in der dichten Live-Wurf-Leiste, um das Layout nicht zu sprengen). `TabletQuickEntryPage.tsx`s starres 22 %/22 %/flex-1-Dreispalten-Layout hat jetzt einen gestapelten Fallback unterhalb von `lg` (Tailwinds erster Breakpoint im gesamten Codebase) — Reihenfolge gestapelt: Strafen/Getränke (am wichtigsten) → Spieler → Übersicht, mit `max-h-[30vh]`-Begrenzung + eigenem Scroll für die Rand-Spalten. PWA-Manifest-`orientation` von `'portrait'` auf `'any'` gelockert, da `TabletQuickEntryPage`/`CameraCapturePage` bewusst Landscape-Kiosk-UIs sind. Stiller Service-Worker-Auto-Update ersetzt durch `registerType: 'prompt'` + eigenem `SKIP_WAITING`-Handshake in `sw.ts`; neuer `useSwUpdate`-Hook + `UpdatePrompt`-Banner (App-Shell, nach `InstallPrompt`) zeigt „Update verfügbar"-Hinweis statt unangekündigt mitten im Kegelabend neu zu laden. Vitest: 4 neue Tests (`useSwUpdate`), volle Suite 1790/1790 grün, `npm run build` clean. |
| 50 | **Bug-Fixes Batch 4**              | ✅      | Strafen-Bearbeiten-Sheet (`ProtocolPage`): (1) Individuelle Strafen (Freitext-Name + Icon, kein passender `PenaltyType`) waren nicht editierbar — das Sheet zeigte nur Schnellstrafen-Chips, `editType` blieb `null`, Icon/Text konnten nur durch eine Schnellstrafe ersetzt werden. Jetzt Schnell/Individuell-Tab-Umschalter analog zum Anlegen-Sheet: `openEditSheet` wählt den Tab automatisch (Eintrag passt zu einem `PenaltyType` → Schnell, sonst Individuell mit vorbefülltem `EmojiPickerButton` + Name-Input); `submitEdit` patcht `penalty_type_name`/`icon` nur bei Änderung, leerer Name deaktiviert Speichern (Backend-PATCH akzeptierte beide Felder schon — reiner Frontend-Fix). (2) Datums-Picker (Admin-Override) zeigte UTC statt lokaler Zeit (`toISOString().slice(0,16)` direkt auf dem Timestamp): neuer Helper `toLocalInputValue` rechnet den Timezone-Offset heraus; beim Submit wird der lokale Input via `new Date(local).toISOString()` als timezone-aware ISO (`Z`-Suffix) geschickt, da das Backend (`_parse_date`) naive Strings als UTC interpretiert (unveränderter Wert wird per Lokal-Format-Vergleich erkannt und nicht gepatcht). 7 neue Vitest-Tests. |
| 51 | **Bug-Fixes Batch 5**              | ✅      | **Abend-Sub-Tabs abgeschnitten:** `EveningHubPage`-Tab-Strip nutzte abweichend vom Rest der App (`ClubAdminPage`/`CommitteePage`/`TreasuryPage`) `flex-1 min-w-0` + `truncate` statt `flex-shrink-0` in einem `overflow-x-auto`-Streifen — bei 4 Tabs + Beenden/Wiedereröffnen-Button wurden Labels auf schmalen Screens abgeschnitten statt scrollbar zu sein. Jetzt gleiches Muster wie die übrigen Tab-Leisten: Tabs behalten ihre natürliche Breite, der Streifen scrollt horizontal. **Mitglieder-Zeilen überladen:** `MembersPage`-Zeilen (App-Nutzer, Spieler-Roster, Gäste) zeigten bis zu 5 icon-only Buttons nebeneinander (📨 ✏️ ⇄ ⬇️ 🔑 ↑/↓ ✕), einige mit unklarer Bedeutung (⇄ Zusammenlegen, ⬇️ Entfernen, 🔗 Verknüpfen nur als Tooltip sichtbar). Buttons entfernt; die ganze Zeile ist jetzt tippbar (`role="button"`, `tabIndex`, `aria-label`, Enter/Leertaste) und öffnet ein Aktions-Sheet mit Icon+Text-Liste (neue `ActionItem`-Komponente) — Zeilen ohne verfügbare Aktion (z. B. nicht-Admin-Ansicht ohne aktiven Abend) bleiben nicht-interaktiv ohne `›`-Chevron. Betrifft auch „Zum Abend hinzufügen", das vorher inline als Button auf Roster-/Gast-Zeilen stand. Neue i18n-Keys: `member.actionsFor`, `member.action.inEvening`, `member.action.linkToRoster`, `member.action.makeAdmin`, `member.action.removeAdmin`, `member.action.deactivate`, `member.action.createInvite`, `member.action.merge`; `member.addToEvening` von Kurzform „Abend" auf vollen Aktionstext „Zum Abend hinzufügen" erweitert (war zuvor nur als `+ Abend`-Button-Suffix genutzt). Vitest: bestehende `MembersPage`-Tests auf das neue Tap-Zeile-→-Aktions-Sheet-Muster umgestellt (68 Tests). |
| 43 | **Strafen × Getränke-Korrelation** | ✅      | Jahresrückblick-Sektion mit 3 Tabs (Default: **Korrelations-Stärke**): diverging Balken pro Mitglied auf einer signierten −1…+1-Skala mit Tick-Beschriftung, Farbe je nach Vorzeichen (grün positiv, rot negativ, muted weak), sortiert nach signiertem r (positiv oben). **Pro Abend** zeigt Scatter € vs Runden + Trendlinie + Pearson r (frontend-recomputed, weil 0-Getränke-Abende ignoriert werden) + Plain-Language-Slope + Strafe-pro-Getränk-Badge "€ Strafe pro Getränk im Jahr" + Quartil-Vergleich Top 25 % vs Bottom 25 % + Top-5 vs ruhigste-5 Streak-Callout ab N≥10 + Saison-Kumulativ-Linie. **Pro Mitglied** ist der Per-(Mitglied×Abend)-Scatter mit `chip`-Pill-Legend zum Fokussieren auf ein Mitglied (einheitlich mit App-Standard-Pills, vorher abweichend rechteckig). Zero-Drink-Abende werden überall im Jahresrückblick gefiltert (Datenlücken, keine echten Nullbeobachtungen) und der Pearson r aus den verbleibenden Abenden frontendseitig neu berechnet. Eigenes Panel in der Abend-Detail-Sektion: Pill-Picker (Mitglied + Bin-Größe 5/15/30 min) mit „Alle"-Pill als Default — Vergleichsansicht stapelt eine Heat-Lane pro Mitglied: Hintergrund = Strafen-€ Δ pro Zeitfenster (amber-Gradient), Linie = aufgelaufene Getränke (Rausch-Pegel); Tap auf eine Lane oder Mitglied-Pill öffnet Fokus-Modus mit Dual-Axis-Line, Δ-Bar-Chart pro Bin und Pearson r der Veränderungen. Pearson-Badge erklärt Richtung („Mehr Strafen → mehr Getränke") + aufklappbarer Was-bedeutet-das-Hinweis. Zusätzlich Strafe-pro-Getränk-Badge (€ Strafe / 🍻, in Euro formatiert via `fe()`) im Abend-Panel: in der Vergleichsansicht der Abendsschnitt, im Fokus-Modus die persönliche Rate inkl. ±%-Vergleich zum Abendsschnitt und ausklappbarer Erklärung. Quote von "Getränke pro €" auf "€ pro Getränk" geflippt — tangiblere Lesart ("jedes Bier kostet 3,20 € Strafe"); Vergleichsfarben gespiegelt: unter Schnitt = grün (billigere Runden), über Schnitt = amber (teurere Runden). Fallback-Ranking im Korrelations-Tab (wenn N&lt;3 Abende) nutzt dieselbe Quote absteigend sortiert (höchste Strafkosten pro Getränk oben), Filter `total_drink_count > 0`. Heat-Lane nur noch einmal pro Abend (im Korrelations-Panel) — frühere Duplizierung in der Verlauf-Sektion entfernt. SVG-Text auf Heat-Lanes (Name + €-Total) nutzt `var(--kce-cream)` statt undefinierter `var(--kce-text)` bzw. amber, sodass nichts mehr in den amber-Heatmap-Hintergrund verschwimmt. `rColor` ist nun vorzeichensensitiv — positives r grün, negatives r rot, |r|&lt;0,2 muted — durchgängig in Heat-Lane-Badge, Pearson-Badge und Korrelations-Stärke-Balken. Korrelations-Panel hat eigene `sec-heading` und `mt-6` Abstand zur Verlauf-Sektion, damit beide nicht visuell zusammenklumpen. GET /stats/correlation/{year} liefert pro Mitglied evening_points; GET /stats/correlation/evening/{evening_id}?bin_minutes. Bier+Schnaps kombiniert. Pearson-Helper (stdlib, n≥3, Varianz-Guard). pytest: 20 Tests; Vitest: lib/stats (pearson + linearRegression + interpretR), API-Client, StatsPage-Tab-Render. **Pro-Abend-Werte normalisiert:** Abwesenheitsstrafen (player_id=None) werden aus der Abendsaggregation ausgeschlossen; Straf-€ und Getränke-Anzahl werden durch die Anzahl anwesender Spieler geteilt → Scatter-Achsen zeigen Ø-Werte pro Spieler. |
| 52 | **🏅 Auszeichnungen & Badges**      | ✅      | Automatisch gesammelte Karriere-Badges im Profil, rein aus vorhandenen Abend-/Spiel-/Strafen-/Getränkedaten abgeleitet (keine neue Tabelle). 12 Badges, davon 8 mehrstufig (Bronze/Silber/Gold): 🎳 Erstkegler, 📅 Stammgast (10/25/50 Abende), 🔥 Serientäter (3/5/10 Abende in Folge — längster zusammenhängender Anwesenheits-Streak in der chronologisch sortierten Vereins-Abendliste), 👑 König (1/5/10×), 🃏 König-Hattrick (3× König in Folge), 🎯 Präsident (Präsidentenspiel gewonnen), 🏆 Seriensieger (10/25/50 Spielsiege, inkl. Team-Siege via `winner_ref`), 9️⃣ Alle Neune (ein Wurf mit `pins>=9`), 🍺 Bierkönig (20/50/100 Bierrunden), 🥃 Hochprozentig (10/25/50 Schnapsrunden), 💸 Pechvogel (50/150/300 € Strafen inkl. count-mode via `_penalty_euro` + Abwesenheitsstrafen), 😇 Saubermann (ein Abend anwesend ohne eigene Strafe). Reine Backend-Funktion `_compute_achievements(evenings, member_id)` in `stats.py` (Tier-Helper `_tiered`/`_binary`, `_longest_run` für Streaks); Rückgabe pro Badge `{key, icon, earned, tier, progress, target}` — Titel/Beschreibung sind i18n-Keys (`achievement.<key>.title/.desc`), Backend liefert nur maschinenlesbare Werte. `GET /stats/me/achievements` + `GET /stats/members/{id}/achievements` (vor `/me/{year}` registriert, sonst parst FastAPI „achievements" als int-Jahr). Frontend: `lib/achievements.ts` (pure: `sortAchievements` earned-first/Tier-sortiert & stabil, `earnedCount`, `progressFraction`), `AchievementShelf.tsx` (4-Spalten-Grid, freigeschaltet = Stufenfarb-Ring + Emoji, gesperrt = ausgegraut + Fortschrittsbalken zur nächsten Stufe) in der ProfileSheet. pytest: 10 Tests; Vitest: 9 lib-Tests + 4 API-Client-Tests. **Nachbesserung (Erklärung on-tap):** Der `title`-HTML-Tooltip auf jeder Badge war auf Touch-Geräten unsichtbar — Mitglieder konnten nicht sehen, was sie tun müssen, um ein Abzeichen freizuschalten. Badges sind jetzt echte `<button>`s; ein Tap wählt die Badge aus (Ring-Highlight) und klappt darunter ein Detail-Panel auf (großes Icon, Titel, „Wie schalte ich das frei?"-Beschreibung, Status: ✓ Freigeschaltet + Stufe bzw. „Noch nicht freigeschaltet" + Fortschrittsbalken `progress/target`); erneuter Tap schließt es. Kopfzeile bekommt einen `achievement.tapHint`-Hinweis. Neue i18n-Keys `achievement.tapHint`/`status.earned`/`status.locked`/`status.nextTier`. Vitest: 5 neue `AchievementShelf`-Tests. |
| 53 | **🎁 Kegel-Wrapped (Jahresrückblick)** | ✅   | „Spotify-Wrapped"-Karten-Story im Profil mit den persönlichen Saison-Highlights, rein aus vorhandenen Daten (keine neue Tabelle). `_compute_wrapped(evenings, member_id, year, year_penalty_by_member)` in `stats.py` liefert: Anwesenheit (`evenings_attended`/`total_evenings`/`attendance_pct`), Strafen-Total+Anzahl, teuerste Einzelstrafe, häufigster Straftyp, König-Anzahl, Spielsiege, Bier-/Schnapsrunden, Wurfschnitt+bester Abend, Strafen-Rang im Verein (`penalty_rank`/`ranked_members` via `_year_penalty_by_member`) und einen abgeleiteten `title_key` (sinner/monarch/beerbaron/champion/loyal/saint/allrounder) für die augenzwinkernde Kegler-Typ-Auszeichnung am Ende. `GET /stats/me/wrapped/{year}` + `GET /stats/members/{id}/wrapped/{year}`. Frontend: reine `lib/wrapped.ts` `buildWrappedCards(stats, fe)` baut den geordneten Kartenstapel (Intro immer zuerst, Kegler-Typ-Finale immer zuletzt, daten­lose Karten übersprungen) und gibt i18n-Keys statt aufgelöster Strings zurück (bleibt pur/testbar); `WrappedDeck.tsx` = Vollbild-Overlay mit Progress-Segmenten, Tap (rechts=weiter/links=zurück), Swipe, Pfeiltasten+Esc, `pop`-Keyframe je Karte. Launcher-Button (🎁) + Deck in der ProfileSheet, nur wenn `has_data`. pytest: 8 Tests; Vitest: 12 lib-Tests + API-Client-Tests. **Nachbesserung (doppeltes Icon):** Mehrere Karten zeigten dasselbe Emoji doppelt — einmal als Hero-Icon und ein zweites Mal am Ende des i18n-Untertexts (König 👑, Siege 🏆, Finale-Titel „Sünder" 😈 / „Bierbaron" 🍺). Die Emojis wurden aus `wrapped.card.king.sub`/`wins.sub` und `wrapped.title.sinner.sub`/`beerbaron.sub` (de+en) entfernt; die Getränke-Karte nutzt jetzt 🍻 als Hero (statt 🍺, das bereits im Wert `🍺 N  🥃 M` steht). |
| 54 | **Profil-Refactor: Einstellungen vs. Meine Saison** | ✅ | `ProfileSheet.tsx` in zwei Sub-Tabs aufgeteilt, gleiches optische Tab-Strip-Muster wie `CommitteePage`/`ClubAdminPage` (`flex gap-1 overflow-x-auto`, aktiver Tab amber). Tab **„🎳 Meine Saison"** (lokaler `useState`, Default): Kontostand & Zahlung/Zahlungsanfragen, Jahres-Statistiken, Wurf-Performance, 🏅 Auszeichnungen-Wand, 🎁 Kegel-Wrapped-Launcher — **kein** Speichern-Knopf. Tab **„⚙️ Einstellungen"**: Avatar (samt Entfernen-Button), Anzeigename/Username/E-Mail, Passwort ändern, Sprache, PWA-Installation, Push-Abo + Kategorie-/Erinnerungs-Präferenzen + Test-Button, Doku-/API-Links, App-Version-Footer, Logout, Konto löschen — der einzige „Speichern"-Knopf (`save()`) ist auf diesen Tab beschränkt. **Abweichung vom ursprünglichen Plan:** kein `useHashTab`/Hash-Deep-Link (`profile:season`/`profile:settings`) — `ProfileSheet` wird app-weit als reiner Overlay-State (`profileOpen` in `App.tsx`) geöffnet, ist an keine Haupt-Seiten-Route gebunden und hatte auch vorher keine Push-Deep-Link-Verdrahtung dorthin; ein einfacher lokaler `useState<'season'|'settings'>('season')` erreicht dieselbe Trennung ohne unbenutzte Hash-Infrastruktur. Wrapped/Achievements-Queries unverändert (`enabled: open && regular_member_id`, laufen unabhängig vom aktiven Tab weiter). Vitest: 67 Tests (4 neue Tab-Wechsel-Tests, 45 bestehende Tests auf `renderProfileSheet({tab: 'settings'})` umgestellt wo sie Einstellungen-Inhalt erwarten), volle Suite 1916/1916 grün, `npm run build` clean. |
| 55 | **Dark/Light/System-Darstellung**       | ✅      | Persönliche Erscheinungsbild-Präferenz, unabhängig von der Feature-#31-Vereinsfarbpalette (Admin-Branding). `applyBgDerivations(bg)` (`App.tsx`) leitete Oberflächen-/Text-Farben schon immer rein aus der *Helligkeit* von `--kce-bg` ab (`l < 50` → dunkel) — Helligkeit spiegeln reicht, kein zweites Farbschema nötig. Neue `resolveThemedBg(baseBg, mode)` spiegelt bei `light` die Helligkeit von `baseBg` (Club-`bg_color` oder Default `#1a1410`) über `hexToHsl`/`hslToHex` (`l → max(100-l, 85)`), behält Farbton/Sättigung bei — Vereinsmarke bleibt auch im Hellmodus erkennbar; Primär-/Sekundärfarbe bleiben in beiden Modi unverändert. Neue `applyTheme(club, theme)` kapselt das und ruft intern `applyClubTheme` mit angepasstem `bg_color` auf — `applyClubTheme` selbst bleibt unverändert (roher Pass-Through, weiterhin von ClubAdminPage's Live-Vorschau der Marken-Farbwahl verwendet, `App.tsx:97-131`). `resolveEffectiveMode(theme)` löst `'system'` per `matchMedia('(prefers-color-scheme: light)')` auf; ein `change`-Listener hält „System" live synchron ohne Reload. Persistenz: `store/theme.ts`, gleiches Muster wie der Locale-Store (`kegelkasse-theme` in localStorage), Default `dark` (keine optische Änderung für Bestandsnutzer, bis aktiv umgeschaltet wird). UI: Dunkel/Hell/System-Zeile in ProfileSheet → Einstellungen, direkt unter der Sprachauswahl, gleiches kompaktes Pill-Button-Muster (nicht die breitere `ModeToggle`-Komponente, die für Panel-weite Umschalter gedacht ist). Bekannte Grenze: die CSS-Var-Ableitung deckt den Großteil der UI ab, aber Kiosk-Ansichten (`CameraCapturePage`, `TabletQuickEntryPage`) bleiben absichtlich hartkodiert dunkel (Blendschutz/Lesbarkeit am Stativ-Tablet), unabhängig vom Nutzer-Theme. Vitest: 8 Tests (`resolveEffectiveMode`/`resolveThemedBg`/`applyTheme` in `App.test.tsx`), 4 Theme-Store-Tests, 4 ProfileSheet-Toggle-Tests. |
| 57 | **Globale Suche**                       | ✅      | 🔍-Icon im Header (ersetzt den bisherigen manuellen Refresh-Button, siehe #56) oder Cmd/Ctrl+K öffnet ein Bottom-Sheet (`GlobalSearch.tsx`, nutzt die geteilte `<Sheet>`-Komponente) mit Live-Filterung über 5 Kategorien: Mitglieder, Konten, Abende, Ankündigungen, Kegelfahrten (bewusst nicht: Abstimmungen/Strafenarten/Spiele — kein fertiges Deep-Link-Ziel). Reine Matching-Logik in `lib/globalSearch.ts` (`searchMembers`/`searchAccounts`/`searchEvenings`/`searchAnnouncements`/`searchTrips`, je auf 5 Treffer gedeckelt, Substring-Match case-insensitive). Navigiert bei Auswahl ausschließlich über bereits bestehende Deep-Link-Hash-Formate — kein neuer Code auf der Zielseite nötig: `#members?memberName=X` (`MembersPage.tsx`), `#treasury:accounts?member=N` (`TreasuryPage.tsx`, bereits vorhanden für PaymentRequest-Push-Links), `#schedule?evening=N` (`SchedulePage.tsx`, expandiert/scrollt/blinkt die Karte, selbes Muster wie Push-Benachrichtigungen), `#committee:announcements?item=N` / `#committee:trips?item=N` (`CommitteePage.tsx`, selbes `item=`-Muster wie Kommentar-Push-Links). Mitglieder- und Konten-Treffer nutzen dieselbe `regularMembers`-Liste (App-Boot-Store) mit zwei Navigationszielen, da auch Gäste ein Konto haben. Evenings/Announcements/Trips werden erst bei geöffnetem Sheet nachgeladen (`enabled: open`). Anzeigename folgt der App-Konvention `nickname \|\| name`. **Nachbesserung (Icons, lesbare Daten, Buchungen-Suche):** Jede Ergebnisgruppe hat jetzt ein Emoji-Präfix (🧑 Mitglieder, 👤 Konten, 📅 Abende, 📒 Buchungen, 📣 Ankündigungen, 🚌 Kegelfahrten — bewusst wiederverwendet aus bereits bestehenden Tab-/Label-Emoji statt neu erfunden, z. B. `treasury.tab.accounts` = „👤 Konten"). Roh angezeigte ISO-Datums-/Zeitstempel (Abende-Titel/-Subtitle ohne Ort, Kegelfahrten-Subtitle) sind durch `formatSearchDate(dateStr, locale)` ersetzt — lokalisierte Langform („15. März 2026" / „March 15, 2026"), datumsreine Strings werden vor dem Formatieren auf lokale Mitternacht verankert (`T00:00:00` statt `Z`), um einen Tages-Versatz nahe der UTC-Grenze zu vermeiden. Die Such-Query matcht jetzt zusätzlich gegen die ausgeschriebene, lokalisierte Monatsform (z. B. „März"/„March" findet alle Einträge dieses Monats) — `searchEvenings`/`searchTrips`/`searchBookings` bekommen dafür ein neues, **erforderliches** `locale`-Argument (`GlobalSearch.tsx` liest es aus `useI18n()`), sodass ein falsches Locale nicht mehr still auf einen Default zurückfällt. **Neue 6. Kategorie „📒 Buchungen"** (`search.bookings`) durchsucht `MemberPayment` (`api.getAllPayments`) + `ClubExpense` (`api.getExpenses`) kombiniert nach Mitgliedsname/Notiz bzw. Beschreibung sowie Datum (roh + ausgeschrieben); `searchBookings()` gibt zwei getrennte `SearchResultKind`s (`payment`/`expense`) statt eines gemeinsamen `booking`-Kind zurück, damit React-Keys (`kind-id`) auch bei zufällig gleicher `id` zwischen einer Zahlung und einer Ausgabe eindeutig bleiben; auf 5 Treffer gedeckelt wie jede andere Kategorie. Kein Einzeldatensatz-Deep-Link nötig — `TreasuryPage.tsx`s bereits vorhandener Buchungen-Textfilter (`bookingSearch`, matcht Mitgliedsname/Notiz/Beschreibung) wird per neuem `?q=`-Hash-Param befüllt (`#treasury:bookings?q=X`, `handleDeepLink` schaltet zusätzlich auf den Buchungen-Tab um), analog zum bereits bestehenden `?memberName=`-Muster. Vitest: `formatSearchDate` + Monats-Namen-Matching + `searchBookings`-Tests (`globalSearch.test.ts`, 31 Tests gesamt), 4 neue `GlobalSearch.test.tsx`-Tests (Icons, Buchungen-Gruppe, lokalisiertes Monats-Matching, Buchungen-Auswahl-Navigation), 2 neue `TreasuryPage.test.tsx`-Tests (`?q=`-Deep-Link schaltet Tab um + befüllt Suche, No-op ohne `q`-Param). **Nachbesserung (Filter-Leiche nach Deep-Link):** Ein per Suchergebnis vorbefülltes Filterfeld (Mitglieder-Suche, Konten-Suche, Buchungen-Suche) blieb beim erneuten Aufruf der Seite stehen und musste manuell gelöscht werden. Neue geteilte `components/ui/SearchInput.tsx` (wrapt `kce-input`, zeigt ein inline ✕-Icon-Button sobald der Wert nicht leer ist, `onChange('')` beim Klick) ersetzt die rohen `<input>`-Felder in `MembersPage.tsx` (Mitglieder-Suche) und `TreasuryPage.tsx` (Konten-/Buchungen-Suche) — bewusst kein automatisches Zurücksetzen beim erneuten Tab-Klick (kein zuverlässiges „Tab erneut gedrückt"-Signal vorhanden, hätte außerdem eine bewusst vom Nutzer gesetzte Suche unerwartet gelöscht), stattdessen ein universeller Ein-Tap-Leeren-Button, der unabhängig von der Herkunft des Filtertexts funktioniert. Neuer i18n-Key `action.clear` (Leeren/Clear). Vitest: 6 neue `SearchInput.test.tsx`-Tests + je ein Clear-Button-Test in `MembersPage.test.tsx`/`TreasuryPage.test.tsx` (Konten- und Buchungen-Tab). **Nachbesserung (Autofokus öffnete die mobile Tastatur nicht zuverlässig):** Der Fokus auf das Sucheingabefeld lief über `setTimeout(..., 50)`, um dem eigenen Fokus-Effekt der `<Sheet>`-Komponente nicht in die Quere zu kommen — mobile Browser (v. a. iOS Safari) öffnen die virtuelle Tastatur bei `.focus()` aber nur zuverlässig, wenn der Aufruf noch im selben Task wie die auslösende Nutzer-Interaktion passiert; ein per Timer verzögerter Aufruf wird oft stillschweigend ignoriert. `setTimeout` entfernt — React flusht Effects kindzu-Eltern innerhalb eines Commits, `Sheet`s eigener Fokus-Effekt (Kind) läuft dadurch ohnehin schon vor `GlobalSearch`s eigenem Effect (Eltern), ein synchroner `inputRef.current?.focus()`-Aufruf gewinnt zuverlässig als letzter. Vitest: 1 neuer Test (Fokus liegt direkt nach dem Öffnen auf dem Eingabefeld, ganz ohne Timer-Vorspulen). |
| 56 | **Pull-to-Refresh statt Refresh-Icon** | ✅      | Vorbereitend für #57: der manuelle Refresh-Button (`handleRefresh` → `queryClient.invalidateQueries()`) war angesichts globaler Defaults `staleTime: 10s` + `refetchOnWindowFocus: true` (`main.tsx`) sowie der bestehenden Sync-Mutations-Konvention weitgehend redundant — sein einziger echter Nutzen war, Änderungen eines *anderen* Geräts/Nutzers zu sehen, ohne die Ansicht zu wechseln. Statt der Fähigkeit ersatzlos verloren zu gehen, wandert der Trigger auf eine Pull-to-Refresh-Geste: neuer `usePullToRefresh`-Hook (`hooks/usePullToRefresh.ts`) hört delegiert auf `<main>` (das selbst nicht scrollt) und löst das tatsächlich scrollende Element pro Touch über `closest('.page-scroll')` auf (die gemeinsame Scroll-Container-Klasse jeder Seite) — keine Änderung an den einzelnen Seiten nötig. Ab 70px Zug bei `scrollTop === 0` löst Loslassen `handleRefresh` aus; ein rotierendes `RotateCw`-Icon (Rotation ∝ Zugweite, `animate-spin` während des Ladens) erscheint mittig über `<main>`. Das freigewordene Icon-Slot im Header übernimmt die Suche — Icon-Anzahl im Header bleibt bei 3 (Suche/Glocke/Avatar), keine zusätzliche Verdichtung. Bottom-Nav bewusst unangetastet gelassen (bereits rollenbasiert auf 6 Ziele dedupliziert, #48) — ein tieferer Merge (z. B. Termine+Ausschuss) würde die `#committee:<tab>?item=`-Hash-Verträge brechen, die sowohl Push-Benachrichtigungen als auch Feature #57 nutzen; das ist als mögliche spätere Vereinfachung vermerkt, nicht Teil dieser Änderung. Vitest: 5 `usePullToRefresh`-Tests (Schwellwert, Deckelung, Aufwärts-Zug ignoriert, gescrollter Container ignoriert), 2 `App.test.tsx`-Tests (Klick öffnet Suche, Cmd/Ctrl+K öffnet Suche). **Nachbesserung (Content bewegt sich nicht mit, Kollision beim Zurückschnappen, Fehlauslösung bei horizontalem Scrollen, bessere Animation):** `<main>` bekommt jetzt einen inneren Content-Wrapper (`transform: translateY(pullDistance)`), der beim Ziehen 1:1 mit dem Finger mitläuft — vorher bewegte sich nur der kleine Indikator, die eigentliche Seite blieb unverändert stehen, was sich kaputt anfühlte. Der Indikator (neue Komponente `PullToRefreshIndicator.tsx`) liegt unbewegt über dem Content-Wrapper und wird dadurch beim Herunterziehen freigelegt statt sich selbst zu bewegen. **Kollision beim Loslassen behoben:** vorher sprang `pullDistance` beim Loslassen sofort auf 0 zurück (während `refreshing` noch lief) und erst beim Abschluss des Refreshs passierte nochmal etwas — zwei unkoordinierte Bewegungen kurz hintereinander. `pullDistance` parkt jetzt bei einem festen Ruhewert (`PARKED = 50`px) solange `refreshing` läuft und fällt erst danach in einer einzigen Bewegung auf 0 zurück; Content-Wrapper und Indikator nutzen dieselbe Transition (`0.25s ease-out`), aktiv nur wenn nicht gerade gezogen wird (`dragging`-Flag, neu im Hook-Rückgabewert), damit beide synchron bleiben. **Fehlauslösung beim horizontalen Scrollen behoben:** Der Hook kannte bisher nur die Y-Bewegung — ein horizontaler Swipe (z. B. auf der scrollbaren Tab-Leiste in `EveningHubPage`) mit leichtem vertikalem Anteil wurde fälschlich als Pull interpretiert. Neue Richtungs-Erkennung (`direction.current`, `DIRECTION_SLOP = 6`px) wartet auf ausreichende Bewegung, bevor sie sich auf „vertikal" oder „horizontal" festlegt; bei horizontal wird die Geste für den Rest des Touches ignoriert (kein Eingriff ins native horizontale Scrollen, da alle Listener weiterhin `passive: true` sind). `touchcancel` bekommt einen eigenen Handler (vorher wiederverwendetes `touchend`) — bricht eine unterbrochene Geste (z. B. eingehender Anruf) immer ohne Refresh-Trigger ab, unabhängig von der Zugweite. **Bessere Lade-Animation:** `RotateCw`-Pfeil ersetzt durch `PullToRefreshIndicator.tsx` mit zwei Zuständen — während des Ziehens ein Chevron, der sich von 0° auf 180° dreht und bei Erreichen des Schwellwerts (`PULL_THRESHOLD`, aus dem Hook exportiert) auf Primärfarbe umspringt („Loslassen zum Aktualisieren"-Signal, analog zu nativen Pull-to-Refresh-Mustern); während des Ladens das bereits vom Boot-Splash bekannte wackelnde Bowling-Pin-Logo (`AppLogoAnimated`) statt eines generischen Spinners — passender zum Markendesign, kein neuer Code für die Animation selbst nötig. Vitest: `usePullToRefresh` auf 8 Tests erweitert (geparktes Warten, horizontales Swipe-Ignorieren, Slop-Schwelle, `touchcancel`), 6 neue `PullToRefreshIndicator.test.tsx`-Tests. |
| 58 | **Bug-Fixes Batch 6**               | ✅      | **VGA fälschlich als „VA" beschriftet:** der Verein-Tab-Untertab zur VGA-Mitgliederverwaltung trug den i18n-Key `club.tab.committee` = „🛠️ VA-Verwaltung" (de.ts) statt „VGA-Verwaltung" — inkonsistent zu allen übrigen VGA-Strings (`committee.isCommittee` = „VGA-Mitglied" etc.). Korrigiert; README- und Doku-Referenzen (`docs/docs/funktionen/vergnuegungsausschuss.md`) ebenfalls von „VA" auf „VGA" vereinheitlicht. **Push-Präferenz „Kommentare & Reaktionen" wurde nie gespeichert:** `PushPreferencesUpdate` (push.py) hatte kein `comments`-Feld — das Profil-Toggle sendete `{comments: bool}` per PATCH, Pydantic verwarf das unbekannte Feld beim `model_dump(exclude_none=True)` still (Toggle wirkte rein kosmetisch, Server-Wert blieb unverändert). Feld ergänzt; pytest: 1 neuer Test (`test_comments_preference_persists`). Antwort-Push-Benachrichtigungen und Deep-Links für Kommentare/Reaktionen auf Ankündigungen/Kegelfahrten/Highlights liefen bei der Untersuchung bereits korrekt (`_notify_new_comment`/`_parent_url` in comments.py) — kein Fix nötig. **Long-Press zeigt jetzt, wer reagiert hat:** `GET /comments/{parent_type}/{parent_id}` und `GET /comments/item-reactions/{parent_type}/{parent_id}` liefern pro Reaktion zusätzlich `users: string[]` (Anzeigenamen, batched via neuem `_names_for_users`-Helper statt Einzel-Query pro Nutzer). Frontend: neuer `useLongPress`-Hook (Pointer-Events, unterscheidet Tap von Halten ab 500ms) + `ReactionPill`-Komponente ersetzt die einfachen Reaktions-Buttons in `ItemReactionBar.tsx` und `CommentThread.tsx` — Tap toggelt weiterhin die eigene Reaktion, Long-Press öffnet ein Popover mit der Namensliste der Reagierenden (leer/kein Popover, wenn niemand reagiert hat). pytest: 2 neue Tests (Kommentar- und Item-Reaktion listen Namen korrekt). Vitest: `useLongPress` (6 Tests) + `ReactionPill` (4 Tests). |
| 59 | **Bug-Fixes Batch 7**               | ✅      | **Pull-to-Refresh: Ladeanimation blieb beim Zurückschnappen sichtbar über dem Content:** `PullToRefreshIndicator` und der darüberliegende Content-Wrapper in `App.tsx` waren beides `position: absolute`-Geschwister ohne explizites `z-index` — die korrekte Deckung des Indikators durch den zurückschnappenden Content verließ sich auf implizite DOM-Reihenfolge-Stapelung, die durch die `transform`-Animation des Content-Wrappers (eigener Compositing-Layer, v. a. unter iOS Safari) nicht zuverlässig eingehalten wurde, sodass das wackelnde Bowling-Pin-Logo während der 0,25s-Rückschnapp-Animation sichtbar über der Seite schwebte statt dahinter zu verschwinden. Beide Elemente bekommen jetzt ein explizites `zIndex` (Indikator `0`, Content-Wrapper `1`), das die Stapelreihenfolge browserunabhängig erzwingt. **Sprachumschalter auf der Login-Seite kollidierte mit der iOS-Notch/Dynamic-Island:** `LoginPage.tsx`s DE/EN-Umschalter war fix mit `top-4` (1rem) positioniert, ohne den `env(safe-area-inset-top)` einer installierten PWA im Standalone-Modus zu berücksichtigen — auf Geräten mit Notch/Dynamic-Island lag der Umschalter unter dem Systembereich. `top` nutzt jetzt `max(env(safe-area-inset-top, 0px), 1rem)`, gleiches Muster wie die bereits bestehenden Safe-Area-Insets in `CameraCapturePage.tsx`/`TabletQuickEntryPage.tsx`. **Deep-Link zu einer Ankündigung/Kegelfahrt füllte das Suchfeld nicht:** `useDeepLinkScroll` (CommitteePage.tsx) scrollte/blinkte den Ziel-Eintrag bereits per `item=`-Hash-Param, ließ das lokale Such-/Filterfeld dabei aber unverändert — kein sichtbarer Hinweis, warum der Eintrag markiert ist, und ein bereits eingetippter, nicht mehr passender Filter hätte das Ziel sogar unsichtbar aus der gefilterten Liste ausblenden können. `useDeepLinkScroll` bekommt einen neuen `setSearch`/`getSearchText`-Parameter und befüllt das Suchfeld beim Auflösen des Deep-Links mit dem Titel (Ankündigungen) bzw. dem Ziel (Kegelfahrten) des Treffers — analog zum bereits bestehenden `?q=`-Muster der Buchungen-Suche (Feature #57); betrifft beide Tabs symmetrisch, da beide denselben Hook nutzen. **Reaktions-Long-Press verlor den Druck bei kleinen Pills, „Wer hat reagiert"-Popover zeigte keine Emojis:** `useLongPress` brach den laufenden Timer bisher bei jedem `pointerleave` ab — auf den kleinen Sekundär-Reaktions-Pills in `CommentThread.tsx` (`text-[11px] px-1.5 py-0.5`, deutlich kleiner als die Herz-Pille) reicht schon geringfügiger Touch-Jitter (die Kontaktfläche eines Fingers ist größer als die Pille), um versehentlich `pointerleave`/`pointerout` auszulösen und den Long-Press-Timer vorzeitig zu canceln — reproduzierbar spürbar als „Press-and-Hold zeigt die Wer-hat-reagiert-Liste nicht, außer beim Herz" (größere Trefferfläche). `pointerleave`-basiertes Abbrechen ist ersetzt durch bewegungsdistanzbasiertes Abbrechen (`MOVE_THRESHOLD = 12`px seit `pointerdown`, per `onPointerMove` gemessen) plus `setPointerCapture` auf dem Zielelement (hält `pointerup`/`pointermove` zuverlässig an diesem Element, auch wenn der Touch-Punkt visuell aus der kleinen Hitbox driftet) — toleriert Jitter innerhalb der Schwelle, bricht aber bei echten Drag-/Scroll-Gesten weiterhin ab. `ReactionPill` bekommt einen neuen `emoji`-Pflicht-Prop und zeigt ihn jetzt neben jedem Namen im Popover (`ItemReactionBar.tsx` + `CommentThread.tsx` reichen ihn für Herz und Sekundär-Reaktionen durch). Vitest: `useLongPress` auf 8 Tests erweitert (bewegungsschwellenbasiertes Abbrechen, Jitter-Toleranz ersetzen den alten `pointerleave`-Test), 1 neuer `ReactionPill`-Test (Emoji neben jedem Namen), 2 neue `CommitteePage`-Tests (Suchfeld-Befüllung Ankündigung/Kegelfahrt). **Nachbesserung (Pull-to-Refresh-Logo tatsächlich noch sichtbar):** Das explizite `zIndex` (Indikator `0`, Content-Wrapper `1`) allein reichte nicht — der Content-Wrapper in `App.tsx` hatte nie eine eigene Hintergrundfarbe, war also selbst bei korrekter Stapelreihenfolge visuell transparent (die einzelnen Seiten malen keinen eigenen vollflächigen Hintergrund), sodass der Indikator unabhängig von Stapelreihenfolge oder Scroll-Position durchschien. `background: 'var(--kce-bg)'` auf dem Content-Wrapper behoben — jetzt deckt er den Indikator tatsächlich ab, sobald er zurückschnappt. |
| 60 | **Bug-Fixes Batch 8**               | ✅      | **Reaktions-Long-Press unzuverlässig auf Touch-Geräten + zeigte nur eine Emoji-Gruppe:** `ReactionPill.tsx` (❤️ + Emoji-Pills in `ItemReactionBar.tsx`/`CommentThread.tsx`) hatte kein `user-select`/`-webkit-touch-callout` gesetzt — auf mobilen Browsern (v. a. iOS Safari) konkurrierte der eigene 500ms-Long-Press-Timer mit der nativen Text-Auswahl/Copy-Callout-Geste auf dem Emoji-Zeichen im Button. Die CSS-Eigenschaften allein reichten nicht, da iOS die native Geste bereits beim Touch-Start einleitet, bevor CSS greift; `useLongPress` ruft jetzt zusätzlich `e.preventDefault()` auf `pointerdown` für `pointerType === 'touch'` (nicht für Maus/Stift), was die native Geste zuverlässig unterbindet, statt sich nur auf `user-select`/`-webkit-touch-callout` zu verlassen. Zusätzlich zeigte das Halten einer Reaktions-Pille bisher nur die Namensliste **dieser einen** Emoji-Gruppe (z. B. Halten auf 😂 zeigte nur „Marius, Tina", nicht auch „😌 Domi") — `ReactionPill` erwartet jetzt `allReactions` (alle Reaktionsgruppen des Items) statt `users`/`emoji` einer einzelnen Gruppe; Halten auf **jeder beliebigen** Pille öffnet dieselbe vollständige, nach Emoji gruppierte Aufschlüsselung (`😂 Marius, Tina` / `😌 Domi`). `ItemReactionBar.tsx`/`CommentThread.tsx` reichen die volle `reactions`/`comment.reactions`-Liste an jede Pille durch statt nur die eigene Gruppe. Vitest: `useLongPress` auf 8 Tests erweitert (Touch-`preventDefault`, Maus/Stift unverändert), `ReactionPill`-Suite auf die gruppierte Aufschlüsselung umgestellt. |
| 61 | **Bug-Fixes Batch 9**               | ✅      | **Reaktions-Popover: Position & Haptik.** Das Reaktions-Popover (`ReactionPill.tsx`) öffnete bisher unterhalb der gehaltenen Pille (`top: rect.bottom + 6`) — auf Touch-Geräten liegt der haltende Finger dort im Weg und verdeckt die gerade geöffnete Namensliste. Anker jetzt von der Viewport-Unterkante aus (`bottom: window.innerHeight - rect.top + 6` statt `top`), sodass die Box unabhängig von ihrer (variablen, je nach Anzahl Reaktionsgruppen) Höhe nach oben über die Pille wächst, ohne die Höhe vorher kennen zu müssen. **Vibrations-Feedback:** `useLongPress` löst jetzt `navigator.vibrate?.(15)` aus, sobald der Hold-Timer feuert (Bestätigung „gehalten, Popover öffnet sich"), und einen zweiten, kürzeren Puls `navigator.vibrate?.(10)` beim Loslassen (`onPointerUp`/`onPointerCancel`) — aber nur, wenn zuvor tatsächlich ein Hold ausgelöst wurde (`firedRef`), damit ein gewöhnlicher Tap nicht ebenfalls vibriert. Dafür ist die Release-Vibration in einer eigenen `release()`-Funktion von der reinen Timer-Aufräum-Funktion `clear()` getrennt — `clear()` wird auch bei zu großer Zeigerbewegung (`move()`, Drag/Scroll-Abbruch) aufgerufen, dort soll kein „Loslassen"-Puls ausgelöst werden, obwohl `firedRef` nach einem bereits erfolgten Hold weiterhin `true` ist. Auf iOS Safari (keine Vibration API) sowie bei Maus/Stift ist `navigator.vibrate` schlicht nicht vorhanden — `?.()` macht das Feature dort folgenlos zum No-op. Vitest: 4 neue `useLongPress`-Tests (Vibration bei Hold-Trigger, bei Loslassen nach erfolgreichem Hold, kein Puls bei gewöhnlichem Tap, Puls auch bei `onPointerCancel`), 1 neuer `ReactionPill`-Test (Popover nutzt `bottom` statt `top`). |
| 63 | **App-Shell-Modernisierung (Bottom-Nav, Side-Rail, Motion)** | ✅ | Modernisierung der UI-Grundlage, die jede Seite erbt — rein `App.tsx` + `index.css` (+ Doku), keine Änderungen an einzelnen Seiten. **Navigation nach unten:** die Haupt-Tab-Leiste saß bisher oben im Header (schwer erreichbar beim einhändigen Bedienen); jetzt eine daumenfreundliche **Bottom-Tab-Bar** (Frosted-Glass via `backdrop-filter: blur` + `color-mix`-transparentem `--kce-bg`, `env(safe-area-inset-bottom)` für die iOS Home-Indicator-Zone, Icons 16→20px, Labels 10→11px, Pressed-State `scale(.94)`). **Ein Nav-Element, zwei Layouts:** ab `1024px` wird dieselbe `<nav class="app-nav">` per CSS-Grid-Area zur **linken Side-Rail** (224px, Zeilen-Layout Icon+Label) — das Root-Layout ist von Flex-Column auf ein `.app-shell`-Grid umgestellt (`grid-template-areas`: mobil `header/main/nav`, ≥lg `nav header`/`nav main`, `max-width` 640→960px auf Desktop), sodass Desktop/Tablet erstmals die Breite nutzt, ohne dass Nav-Labels doppelt im DOM stehen (Test-Kompatibilität: `getByText('nav.*')` matcht weiterhin genau einmal). Kiosk-Overlays (`TabletQuickEntryPage`/`CameraCapturePage`, `position:fixed` z≥60) überdecken die Bottom-Bar unverändert. **Seiten-Wechsel-Animation:** jede der 7 always-mounted Seiten-Wrapper trägt jetzt `.page-pane` (Fade+6px-Slide-Up, 0.22s) — der bestehende `display:none`→`block`-Toggle startet CSS-Animationen automatisch neu, daher kein JS/Router-Umbau nötig; `prefers-reduced-motion: reduce` deaktiviert `.page-pane`/`.animate-slide-up`/`.animate-fade-in`. **Interaktion/Design-Polish:** globaler Keyboard-Fokusring via `:focus-visible` (2px `--kce-primary`, kein Touch-Rauschen), `.kce-card` Radius 14→16px + dezente Elevation, `.sheet-panel` `rounded-t-3xl` + Schatten, `.field-label` 10→11px, Header-Subtitle 9→10px, Fake-Gradients entfernt (`linear-gradient(bg,bg)` im Header, `penalty-strip` primary→primary → solid), irreführender „Lazy-loaded"-Kommentar in `App.tsx` korrigiert (Imports sind statisch). Vitest: 3 neue App-Shell-Tests (Nav außerhalb des Headers + nach `<main>` in DOM-Reihenfolge, 7 `.page-pane`-Wrapper, `.app-shell`-Grid-Struktur), volle App-Suite 55/55 grün. |
| 62 | **🙈 Halle der Schande & 🎉 Feier-Effekte** | ✅ | Zwei rein spaßorientierte Ergänzungen, beide reines Frontend (kein Migration/Backend-Change). **Halle der Schande:** Neue Karte im Jahr-Tab von `StatsPage.tsx`, direkt unter dem 🥇 Jahres-Podium — vier „Worst of"-Awards pro Saison, rein aus den bereits vom `GET /stats/year/{year}`-Endpoint gelieferten Spielerdaten abgeleitet (`computeHallOfShame` in `lib/stats.ts`, analog zum bestehenden Muster des Abend-Hall-of-Fame): 💸 **Strafen-Quote** (höchste `penalty_total/evenings`, min. 3 Abende), 🍻 **Durstigster Kegler** (meiste Bier+Schnaps-Runden), 🎯 **Schwächster Wurf-Schnitt** (niedrigster `avg_pins`, min. 10 Würfe), 🃏 **Ewiger Zweite** (meiste Abende ohne einen einzigen Sieg, min. 3 Abende) — jede Kategorie ohne ausreichende Stichprobe wird ausgeblendet statt einen irreführenden Sieger zu zeigen. Ausgeblendet während aktiver Mitgliedersuche (gleiches Verhalten wie das Podium); Antippen einer Zeile öffnet das bestehende `PlayerDetailSheet`. **Feier-Effekte:** Konfetti-Burst + kurzer Synth-Sound (`lib/celebrate.ts`, reines Canvas + Web-Audio-API, keine neue Abhängigkeit) feuert bei zwei Momenten — König gekrönt (`is_opener`-Spiel mit Spieler-Sieger, an allen drei Beenden-Stellen: `GamesPage.tsx`, `TabletQuickEntryPage.tsx`, `CameraCapturePage.tsx`) und Alle Neune (Wurf mit `pins >= 9`, an allen Wurf-Erfassungsstellen inkl. Kiosk-Kamera-Loop, manueller Bestätigung, Test-Wurf-Button und der Tablet-Auto-Advance-Diff-Logik) — inkl. Toast (`showToast`) und einem extra hohen Ton speziell für Alle Neune. Respektiert `prefers-reduced-motion` (kein Konfetti, Ton bleibt) und einen neuen persönlichen Ein/Aus-Schalter „🎉 Feier-Effekte" in ProfileSheet → Einstellungen (`store/effects.ts`, gleiches `persist`-Muster wie der Dark/Light/System-Store aus Feature #55, Default an). Web-Audio-`AudioContext` wird beim ersten Tastendruck irgendwo in der App vorgewärmt (statt erst im `celebrate()`-Aufruf), da dieser oft nach einem `await api.finishGame(...)` liegt und Safaris Autoplay-Gate spät gestartetes Audio sonst blockieren kann. pytest: keine (rein Frontend). Vitest: 9 neue `computeHallOfShame`-Tests, 4 neue `StatsPage`-Tests, 6 neue `celebrate.ts`-Tests, 4 neue `effects`-Store-Tests, 4 neue `ProfileSheet`-Toggle-Tests, 8 neue Celebrate-Integrationstests über `GamesPage`/`TabletQuickEntryPage`/`CameraCapturePage`. |
| 64 | **Router-Migration & echtes Lazy Loading** | ⬜ | Fundament für alle weiteren UX-Phasen (Fortsetzung von #63). Die homegrown Hash-Protokolle (`#treasury:accounts?member=N`, `#committee:trips?item=5`, `#evening:manage`, `?q=`, `?memberName=`) durch einen echten Router mit typisierten Routen ersetzen (TanStack Router passt zum bestehenden TanStack-Query-Stack) — Push-Benachrichtigungen, Globale Suche und Cross-Page-Links nutzen dann benannte Routen statt String-Verträge, und IA-Änderungen sind keine Breaking Changes mehr. Alte Hash-URLs bekommen Redirects (Push-Deep-Links im Umlauf bleiben gültig). Ein geteiltes „navigate-and-highlight"-Primitive ersetzt die ~5 Kopien der `deepLinkItemId`/`flashTimerRef`-Scroll-Flash-Logik (EveningHubPage, CommitteePage, TreasuryPage, SchedulePage, …). Seiten werden per `React.lazy` tatsächlich code-gesplittet (heute statisch importiert + always-mounted trotz gegenteiligem Kommentar); Scroll-Restauration übernimmt der Router. |
| 65 | **Abend-Modus (Live-Takeover)** | ⬜ | Journey-basierte IA statt Feature-Tabs: Läuft ein Kegelabend, pivotiert die ganze App in einen immersiven Live-Modus statt „ein Tab von sechs" zu sein (die AKTIV-Pille deutet das heute nur an). Großer Live-Scoreboard-Header (aktuelles Spiel, wer ist dran, letzter Wurf), Live-Ticker der Ereignisse (Strafen/Getränke/Würfe/Alle Neune chronologisch einfließend, via bestehender SSE/Polling-Infrastruktur), daumengroße Schnellaktionen (Strafe, Runde, Highlight-Foto), Bottom-Nav reduziert auf „Abend / Rest der App". Beim Beenden des Abends kehrt die normale Navigation zurück. Tablet-Schnellerfassung und Kamera-Kiosk bleiben eigenständige Spezialansichten. |
| 66 | **Start-Dashboard „Für dich"** | ⬜ | Personalisierte Startseite als Default-Landing ohne aktiven Abend (heute landet man auf dem Abend-Tab, der dann nur ein Start-Formular zeigt): nächster Termin mit Inline-RSVP, eigener Kontostand mit Bezahl-Aktion, ungelesene Community-Neuigkeiten (Ankündigungen/Highlights), persönliche Saison-Kennzahl (z. B. Wurfschnitt-Trend), Schnellzugriffe. Speist sich komplett aus bestehenden Endpoints (schedule, my-balance, committee, stats/me) — reine Kompositions-/IA-Arbeit, kein neues Backend. |
| 67 | **Kasse: Blick vs. Analyse** | ⬜ | Die Kasse-Übersicht (2.280 Zeilen, 33 useState) in zwei Ebenen entflechten: **Blick** = eine Karte pro Kernfrage („Was schulde ich?" / „Wer schuldet noch?" / „Was ist in der Kasse?"), jede per Tap aufklappbar zur Detailebene. **Analyse** = eigene Drill-in-Ansicht für Verlauf-Graph, Spieler-Filter mit Austritts-Simulation (Write-off/Refund/Share — Admin-Powertools, keine Mitglieder-Features) und Geldfluss-Aufschlüsselungen. Das wiederkehrende „collapsed by default"-Pflaster (Erklärkarten, Chart-Karten, Filter-Karten) wird durch echte Informationsarchitektur ersetzt statt Dichte pro Feature zu verstecken. Alle bestehenden Deep-Links (`member=`, `q=`) bleiben gültig (nach #64 als Router-Redirects). |
| 68 | **Statistik-Labor** | ⬜ | `StatsPage` (3.133 Zeilen) aufteilen: Der Jahr-Tab behält Ranking, Podium, Halle der Schande und die persönlichen Kennzahlen (Glance-Ebene); das Strafen×Getränke-Korrelations-Panel (Pearson r, Quartile, Heat-Lanes, Bin-Picker) zieht in ein eigenes, bewusst betretenes **📊 Statistik-Labor** (opt-in Destination statt Scroll-Ballast). Dort ist dann auch Platz für Neues: Vereinsrekorde-Seite (längste Serie, teuerster Abend, beste Saison), Kopf-an-Kopf-Vergleich zweier Mitglieder, Saison-Vergleich über `season_snapshot`-Daten. |
| 69 | **Komponenten-Konsolidierung** | ⬜ | Die in CLAUDE.md als Prosa-Konventionen beschriebenen Muster werden echte Komponenten: `<MemberRow>` (Avatar + Kegelname + Ich-Badge + Pins/Präsident-Icons + Trailing-Slot — heute pro Seite reimplementiert), `<StatTile>`, `<ExpandableCard>` (FlowRow/Erklärkarten/Chart-Karten handrollen alle denselben Aufklapp-Zustand), `<ActionMenu>` (⋯-Menü-Muster aus #51 verallgemeinert). ProfileSheets private Bottom-Sheet-Implementierung (1.000 Zeilen Overlay, das eigentlich eine Seite ist) wird eine echte Route auf der geteilten `<Sheet>`/Page-Basis — ein Fokus-Management statt zwei. Danach kann eine Konvention lauten „nutze `<MemberRow>`" statt drei Absätze Verhaltensbeschreibung. |
| 70 | **Semantische Tokens & kontrastsicheres Theming** | ⬜ | Von rohen Farb-Vars (`--kce-cream`, `--kce-surface2`) zu semantischen Paaren (`--surface`/`--on-surface`, `--accent`/`--on-accent`, `--team-N`/`--on-team-N`), deren Hell-Modus-Ableitung kontrastgeprüft berechnet wird (Luminanz-Formel wie in `email_theme`) statt nur die Hintergrund-Helligkeit zu spiegeln — heute passieren Primär-/Sekundärfarbe und die sechs hartkodierten `team-N`-Farben sowie `btn-danger`-Rot den Hellmodus ungeprüft und können unlesbar werden. Verbliebene Hex-Literale (`#c4701a`-Avatar-Gradient, Role-Badges) auf Tokens umziehen. Ergebnis: Vereins-Branding × Dark/Light kann konstruktionsbedingt keine unlesbaren Kombinationen mehr erzeugen. |
| 71 | **Typo- & Dichte-Pass über alle Seiten** | ⬜ | Fortsetzung des Shell-Anfangs aus #63 in die Seiten hinein: die ~290 Verwendungen von 9–11px-Text auf eine echte Typo-Skala heben (min. 12px für Lesbares, 14px Body) — Hierarchie über Gewicht, Farbe und Abstand statt über Kleinheit ausdrücken. Seitenweise vorgehen (Kasse → Abend → Stats → Rest), pro Seite Screenshots vorher/nachher, bestehende Vitest-Suites als Verhaltens-Guard. Zielgruppe Kegelverein: eher ältere Augen als das typische PWA-Publikum — Lesbarkeit ist hier Kernfeature, nicht Politur. |
| 72 | **Motion-System & View Transitions** | ⬜ | Einheitliches Bewegungssystem statt Einzel-Keyframes: geteilte Dauer-/Easing-Tokens, **View Transitions API** für Shared-Element-Übergänge (Karte → Detail-Sheet morpht statt hart zu schneiden; Progressive Enhancement, Fallback = heutige Animationen), animierte Zahlen-Count-Ups für Geldbeträge und Punktestände (die Kasse fühlt sich lebendig an, wenn der Kontostand hochzählt), **Skeleton-Loading** statt Spinner auf allen Listen/Karten (wahrgenommene Geschwindigkeit), Haptik-Muster (`navigator.vibrate`-Vokabular: Erfolg/Fehler/Auswahl, aufbauend auf dem Long-Press-Feedback aus #61). Alles hinter `prefers-reduced-motion` + dem 🎉-Effekte-Schalter aus #62. |
| 73 | **Icon-Sprache & Touch-Affordanz-Audit** | ⬜ | Eine Regel, überall angewandt: **Lucide-Icons für Bedienelemente** (Buttons, Tabs, Aktionen — immer mit Textlabel), **Emoji für Inhalte** (Strafenarten, Badges, Feier-Momente — die Persönlichkeit der App). Die Bug-Fix-Batches #51/#52/#58–61 haben dieselbe Lektion wiederholt gelernt (icon-only Buttons unklar, `title`-Tooltips auf Touch unsichtbar, Long-Press kämpft mit iOS) — daraus wird ein präventives Audit: jede Affordanz sichtbar und antippbar, Long-Press nur je als Abkürzung zu etwas auch per Tap Erreichbarem, versteckte Aktionen in sichtbare ⋯-Menüs. |
| 74 | **📺 Beamer-/TV-Scoreboard** | ⬜ | Read-only Vollbild-Ansicht zum Casten/Anzeigen auf TV oder Beamer an der Kegelbahn (gleiche Kiosk-Muster-Familie wie `CameraCapturePage`): Live-Spielstand mit Riesen-Typo, wer ist dran, Wurf-Historie des laufenden Spiels, Zwischenstand-Rotation (Strafen-Ranking des Abends, Getränke-Zähler, letztes Highlight-Foto), König-/Alle-Neune-Vollbild-Feier mit den #62-Effekten. Zugriff via Secret-Token-Link (Muster wie iCal-Export #24), kein Login auf dem TV-Gerät nötig; speist sich aus den bestehenden Evening-/SSE-Endpoints. |
| 75 | **Desktop-Master-Detail** | ⬜ | Aufbauend auf der #63-Side-Rail: ab `lg` nutzen Listen-Seiten die Breite als Zweispalter (Liste links, Detail rechts statt Bottom-Sheet) — Historie (Abend-Liste + Abend-Detail), Kasse-Konten (Konten + Zahlungs-Historie), Mitglieder (Roster + Aktionen/Profil). Bottom-Sheets bleiben das Mobile-Muster; dieselbe `<Sheet>`-API rendert auf Desktop als Detail-Panel oder zentrierter Dialog (responsive Variante der Komponente, kein Fork pro Seite). |
| 76 | **Onboarding, App-Shortcuts & Sprach-Schnellerfassung** | ⬜ | **Onboarding:** geführte 3-Schritte-Tour beim ersten Login (Rolle erkennt, was gezeigt wird: Mitglied = RSVP/Kasse/Profil, Admin = Abend starten/Strafen/Verwaltung), wiederaufrufbar im Profil. **Manifest-Shortcuts:** Long-Press aufs App-Icon → „Strafe erfassen" / „RSVP" / „Mein Konto" (PWA `shortcuts`, deep-linked via Router aus #64); **Share Target:** Fotos vom System-Share-Sheet direkt als Abend-Highlight teilen. **Sprach-Schnellerfassung (Experiment):** Mikro-Taste in der Schnellerfassung — „Marius Pudel" wird per Web Speech API gegen Spieler-Roster + Strafenkatalog gematcht und als vorausgefüllter Bestätigungs-Dialog angezeigt (nie blind gebucht); Fallback bei fehlender Browser-Unterstützung: Taste erscheint nicht. |

## Testing TODO

The following functionality is **not yet covered** by automated tests and should be added in future iterations:

### Backend (pytest)

- **Reminders** — APScheduler job logic (debt, RSVP, schedule reminders) — requires time-travel mocking
- **Push broadcast triggers** — notification triggers for König, Abend start, new members, etc.

### Frontend (Vitest)

- **CameraCapturePage** — RAF/camera loop auto-submit (requires real MediaStream + canvas mocking), confirmation countdown timer (requires fake timers)

### Already covered

**Backend (pytest):** Games (create/start/finish/delete, loser-penalty, king flag, throw log CRUD, active-player) · Drinks · Stats (year/me/throws, member throws, penalties×drinks correlation year + within-evening derivative — 17 new tests) · Auth (login, register, reset, profile, avatar, locale, delete) · Push (subscribe/unsubscribe/status/test/preferences/debug/recent/mark-read/trigger-reminders) · Club routes (CRUD, logo upload) · Evenings (CRUD, players, penalties) · Schedule (CRUD, RSVP, iCal) · Treasury (balances, payments, expenses, PaymentRequest flow, treasury-payout — 6 tests) · Committee (announcements, trips, polls — CRUD, vote, retract, close/reopen) · Superadmin (club CRUD, switch-club, listing) · Comments (list/create/edit/delete, reactions, item-reactions) · Uploads (media upload) · Reports (xlsx+pdf export) · Sync (add/delete penalty+drink) · Backups (CRUD with mocked pgbackrest) · Season (list/get snapshots, close season — balance carry-over, evening archive, duplicate guard, role guards — 14 tests) · Club member removal cascade + reactivation (8 tests)

**Frontend (Vitest):** cameraEngine (digit/pin/lamp) · turnOrder (alternating/block) · lib/stats (pearson, linearRegression, interpretR — 14 tests) · API client — all 80+ api.* methods (URL + HTTP method + body), authState, error classes, 401/NetworkError dispatch, uploadMedia, downloadReport, flushOfflineQueue (empty queue, single item, tempId remapping, server error discard, queue-changed/sync-flushed events), season methods (listSeasonSnapshots/getSeasonSnapshot/closeSeason) · Store/app.ts (isAdmin, role helpers) · i18n key parity (de↔en) · hexToHsl / hslToHex round-trip · offlineQueue (enqueue/getAll/remove/count/clear, isQueuableMutation) · Error handling (UnauthorizedError, NetworkError, OfflineQueuedError, authState) · Toast (showToast, ToastContainer render/display/timeout/multi/cleanup) · EmojiPickerButton (icon/insert mode, open/close, emoji selection) · CommentThread (Avatar with src/initials, toggle, controlled/uncontrolled, add/delete/react, comment rendering) · usePwaInstall (iOS UA detection, standalone matchMedia, beforeinstallprompt capture, promptInstall accepted/unavailable — 6 tests) · useActiveEvening (no-ID, real ID, temp/pending ID, invalidate, temp-id-resolved event, SSE setup) · useEveningList (query delegation) · StatsPage (annual ranking, year selector) · EveningPage (start form, active/closed states, player add/remove/edit, team add/edit/delete, close/reopen flow, highlights, attendance sheet) · TreasuryPage (overview/accounts/bookings tabs, payment recording, PaymentRequest confirm/reject) · SchedulePage (RSVP toggle, iCal copy, scheduled evening list) · HistoryPage (close/reopen evening, backlog sheet, expanded detail) · MembersPage (member CRUD, invite link, roster link, search, role/deactivate) · ProfileSheet (basic display, push preferences, balance display, payment form, logout) · TabletQuickEntryPage (rendering, player selection, penalty logging, drink logging, finish-game flow, throw strip, event deletion, start game) · CameraCapturePage (rendering, camera error, mode toggle, calibration save/reset, detection mode, test-throw submission, game finish, kiosk enter/exit) · GamesPage (game CRUD, start/finish flow, king flag) · CommitteePage (announcements, trips, polls — render, vote, retract, create, delete, close/reopen) · ClubAdminPage · LoginPage · ProtocolPage (penalty log) · SeasonTab (landing/preview/confirm/done wizard flow, carry-over preview, close action — 17 tests)
